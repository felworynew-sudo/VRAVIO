// Без этого атрибута собранное приложение открывает позади себя консольное
// окно: на Windows подсистема по умолчанию — консольная, и пользователь видит
// чёрный прямоугольник, которого никто не просил. В отладочной сборке консоль
// оставлена намеренно — там она и есть способ увидеть panic.
#![cfg_attr(not(debug_assertions), windows_subsystem = "windows")]

fn main() {
    vravio_lib::run()
}
