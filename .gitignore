node_modules/
dist/
coverage/
.vite/
*.log
*.tsbuildinfo
.DS_Store
Thumbs.db
output/
research/
backup_*.ts
backup_*.tsx
.playwright-cli/

# Model weights are fetched at runtime, not committed and not built in — see
# the note on `url` in apps/web/src/ml/inpaint/definitions/. This entry stays
# so a local copy dropped here for offline work cannot be committed by
# accident: `icons/` is the Vite `publicDir`, so anything in it is copied
# verbatim into `dist`.
icons/models/
