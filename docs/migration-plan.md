# ТЗ: переход на каталожную структуру

Документ описывает переход VRAVIO от «архитектуры в компоненте» к каталожной,
при которой добавление инструмента, окна, команды, правила или плагина — это
**новый файл в известном каталоге**, а не правка существующего кода.

Язык документа — русский, идентификаторы и имена файлов — английские, как во
всём репозитории.

**Как пользоваться.** Каждый этап заканчивается состоянием, в котором проект
собирается, проходит тесты и работает. Между этапами можно остановиться на
любой срок. Отмечайте выполненное прямо здесь: `- [x]`. Строка считается
закрытой, только когда есть и реализация, и автопроверка — то же правило, что в
`docs/requirements.md`.

**Чего этот документ не делает.** Он не меняет поведение редактора. Ни один
этап не должен менять то, что видит пользователь, кроме явно оговорённых
(панель инструментов с `(...)`, окно загрузки). Всё остальное — перекладка.

---

## 1. Инвентарь на момент составления

Цифры получены измерением репозитория, а не оценкой. Они — база сравнения:
после перехода ни одна из них не должна ухудшиться.

### Объём

| | значение |
|---|---|
| Всего TS/TSX | 21 331 строка |
| `apps/web/src/RasterWorkspace.tsx` | 2 068 строк |
| `apps/web/src/DockLayout.tsx` | 621 |
| `apps/web/src/App.tsx` | 572 |
| `packages/env-raster/src/render.ts` | 492 |
| `apps/web/src/commands.ts` | 294 |
| `apps/web/src/store.ts` | 246 |
| `apps/web/src/tools.ts` | 92 |
| `apps/web/src/i18n.ts` | 11 |
| Тесты | 396 в 25 файлах |
| Команд в рантайме | 80 |
| Инструментов | 30 (24 растр + 6 вектор) |
| Иконок в `icons/` | 130 |

### Что уже устроено правильно

Это фундамент перехода — его не переделываем, а тиражируем.

- **Три каталога на `import.meta.glob`**: `raster-adjustments/`,
  `raster-core-panels/`, `vector-core-panels/`. Каждый — тройка
  `types.ts` / `registry.ts` / `runtime.ts` + `definitions/`.
- **Ядро вне React**: `document-store`, `history-manager`, `command-registry`,
  `asset-store`, `keymap-manager` в `packages/kernel`.
- **Реестр команд** с `id`, `label`, `category`, `shortcut`, `scope`,
  `isEnabled`, `execute` — контракт уже есть. В рантайме зарегистрировано **80
  команд** из 52 мест в коде: File 12, Layer 15, Tools 23, View 9, Select 6,
  Edit 5, Image 5, 3D 2, Object 2, Filter 1.
- **Круговой обмен между средами** через ревизии ассетов, 27 тестов.
- **Правило выделения** — `confineToSelection` в трёх точках.
- **Ленивая загрузка ONNX** — нейросети не в основном бандле.
- **Автогенерация панели настроек** из схемы `ToolOption`.
- **Автоматическая регистрация шорткатов** по группам инструментов.

### Что мешает

**Главное.** Поведение инструментов не в файлах инструментов. В
`RasterWorkspace.tsx` — **49 ветвлений по `activeToolId`**, размазанных с
795-й по 2025-ю строку. Логика одного инструмента лежит в пяти разных местах:

| инструмент | строки |
|---|---|
| `raster.clone` | 795, 1167, 1644, 1857, 2025 |
| `raster.move` | 1076, 1304–1310, 1326, 1944 |
| `raster.burn` | 801, 1247, 1356, 1661, 1735 |

**Остальное:**

- `tools.ts` — 30 инструментов одним массивом в одном файле; описание
  декларативное, поведения нет.
- `commands.ts` — 294 строки одним файлом, без разделения по средам.
- **i18n сломан в определениях инструментов**: подпись зашита строкой
  `"Brush Tool (Кисть)"`. При этом в каталоге панелей уже сделано правильно —
  `title: { en, ru }`. То есть образец в репозитории есть, надо распространить.
- Нет: плагинов, сценариев, кистей, паттернов, JSON темы, движка правил,
  бэкапов с выбором места, десктоп-сборки.

---

## 2. Границы дисциплины

Самое важное решение всего перехода. Атомарный дизайн применяется **не ко
всему**, иначе производительность упадёт.

| слой | дисциплина | почему |
|---|---|---|
| Интерфейс: поля, ползунки, окна | Атомарный дизайн: атом → молекула → организм | Переиспользование и предсказуемость важнее микросекунд |
| Каталоги: инструменты, окна, команды, правила, плагины | Декларативные данные + реестр по glob | Расширение файлом, а не правкой |
| Движок: пиксели, композит, тайлы, маски | Data-oriented: плоские типизированные массивы, батч-операции | Вызов на пиксель при 2 Мп/кадр дороже всей полезной работы |

**Атомарность движка — это атомарность операций, а не вызовов.** Одна операция
= один файл, экспортирующий функцию над целым буфером. Внутри неё цикл остаётся
плотным, без обратных вызовов и без диспетчеризации по строке.

Все выигрыши, достигнутые проектом, получены снятием абстракции в горячем
пути — компоновкой по тайлам вместо целого холста, хранением слоя в его
собственных границах вместо размера канваса, блочным копированием вместо
двух проходов композита при перемещении, перезаписью только изменившегося
буфера при автосохранении. Переход не имеет права вернуть ни один из этих
приёмов.

**Цифры перепроверены этапом 0** (раздел 5, этап 0) на бенчмарке
`packages/env-raster/src/performance.bench.test.ts` и
`packages/kernel/src/performance.bench.test.ts`, а не взяты по памяти из
более ранней работы, как было при первой редакции этого документа — тогда
композит оценивался в ~14 мс, перемер на реалистичной фикстуре (21 слой,
1920×1080, слои размером с типичный мазок, а не во весь холст) дал ~27 мс.
Расхождение не опровергает саму оптимизацию: композит документа с 46
слоями во весь холст без тайлов стоит 428 мс, с тайлами — 38 мс (это
задокументировано прямо в коде, `render.ts`, комментарий у
`subdivideAbove`) — четырёхкратный запас у порога бенчмарка (×6 от
измеренного) рассчитан от актуального числа, а не от цифры, которая не
подтвердилась.

---

## 3. Целевая структура

```
apps/web/src/
  ui/                        # атомарный дизайн, среда-независимо
    atoms/                   #   NumberBox, Slider, Checkbox, Select, Button,
                             #   ColorSwatch, EyedropperButton, Icon
    molecules/               #   SliderWithNumber, LabeledField, OptionRow
    organisms/               #   OptionsBar, ModalFrame, ContextMenu
    overlays/                #   прицелы, линейки, муравьи, направляющие,
                             #   гриды, манипуляторы, плашки-значения
    theme/
      tokens.json            #   ВСЕ цвета и размеры числами
      tokens.ts              #   типизированное чтение + проверка полноты

  environments/              # каталог сред; новый подкаталог = новая среда
    raster/
      environment.ts         #   манифест среды (см. 4.6)
      tools/
        definitions/         #   по файлу на инструмент
        registry.ts
        types.ts
      windows/
        definitions/         #   по файлу на окно
        registry.ts
        runtime.ts
        types.ts
      commands/
        definitions/
        registry.ts
      adjustments/           #   переезжает из raster-adjustments/
      rules/                 #   правила, специфичные для растра
      plugins/
    vector/                  #   та же форма
    audio/                   #   та же форма, когда появится
    video/

  modals/                    # общие модальные окна
    definitions/             #   new-document, error, confirm, progress, about
    registry.ts

  rules/                     # движок правил + общие правила
  scripts/                   # сценарии: запись, хранение, воспроизведение
  assets-library/
    brushes/
    patterns/
  ml/                        # нейросети, по файлу на задачу
  platform/                  # web / tauri: файлы, темпы, шрифты, буфер обмена

packages/
  kernel/                    # без изменений по сути
  engine-raster/             # бывший env-raster
  engine-vector/             # бывший env-vector
```

Каталог считается средой, если содержит `environment.ts` нужной формы. Ничего
больше регистрировать не нужно.

---

## 4. Контракты

Контракт — это то, что позволяет реестру подхватить файл. Каждый описан ниже с
обоснованием полей.

### 4.1 Инструмент

Во всех контрактах ниже `label: LocalizedText` — тип из
`apps/web/src/i18n.ts`, добавленный на этапе 1: `{ en: string; ru?: string;
uk?: string; es?: string; de?: string; ja?: string; zh?: string }`, `en`
обязателен, остальные падают на английский через `resolveLabel`.

Набор хуков **выведен из кода**, а не придуман: это те функции, в которых
сегодня стоят 49 ветвлений, с числом ветвлений в каждой.

| сегодняшняя функция | ветвлений | хук контракта |
|---|---|---|
| `handlePointerDown` | 30 | `onPointerDown` |
| `appendBrushPoint` | 6 | `onStrokePoint` |
| `finishGesture` | 5 | `onGestureEnd` |
| `onDropModel` | 3 | `onDrop` |
| `handleWheel` | 2 | `onWheel` |
| `updateBrushCursor` | 1 | `cursor` |
| `beginNavigation` | 1 | `onNavigationStart` |
| `handlePointerMove` | 1 | `onPointerMove` |

```ts
export interface ToolDefinition {
  id: string;                       // "raster.brush"
  order: number;                    // положение в панели по умолчанию
  group?: string;                   // общая ячейка панели и общая буква
  label: LocalizedText;
  icon: string;                     // имя файла в icons/
  shortcut?: string;

  options: readonly ToolOption[];   // как сейчас — панель строится сама

  /** Типы слоёв, на которых инструмент работает. Заменяет RASTER_ONLY_TOOLS. */
  appliesTo: readonly RasterLayerKind[];
  /** Что делает с документом. Читается движком правил. */
  touches: "pixels" | "selection" | "view" | "vector" | "none";
  /** Требует растеризации нерастрового слоя перед работой. */
  requiresRasterized?: boolean;

  /** Прицелы. Первый — по умолчанию; переключение по контексту или клавише. */
  cursors: readonly CursorRef[];

  /** Окно тонкой настройки, если ползунков на панели мало. */
  settingsModal?: string;           // id из modals/
  contextMenu?: readonly string[];  // id команд для правого клика по холсту

  onPointerDown?(ctx: ToolContext, event: ToolPointerEvent): void;
  onPointerMove?(ctx: ToolContext, event: ToolPointerEvent): void;
  onStrokePoint?(ctx: ToolContext, point: StrokePoint): void;
  onGestureEnd?(ctx: ToolContext): void | Promise<void>;
  onWheel?(ctx: ToolContext, event: ToolWheelEvent): boolean;
  onNavigationStart?(ctx: ToolContext, event: ToolPointerEvent): boolean;
  onDrop?(ctx: ToolContext, files: readonly File[]): void | Promise<void>;
  onKey?(ctx: ToolContext, event: KeyboardEvent): boolean;
  onDeactivate?(ctx: ToolContext): void;   // обязателен, если есть состояние
}
```

`ToolContext` — единственная дверь инструмента наружу. Он **не даёт** прямого
доступа к React-состоянию: только документ, вьюпорт, настройки, выделение и
метод фиксации правки. Это и есть то, что делает правило невозможным обойти.

```ts
export interface ToolContext {
  readonly document: RasterDocumentState;
  readonly viewport: Viewport;
  readonly options: Record<string, unknown>;   // настройки этого инструмента
  readonly selection: PixelSelection | null;
  readonly activeLayer: RasterLayer | null;
  /** Единственный путь к пикселям. Проводит правку через движок правил. */
  commit(edit: PixelEdit): Promise<void>;
  /** Всё, что рисуется поверх холста, — только отсюда. */
  overlay: OverlayHandle;
  run(commandId: string, args?: unknown): Promise<void>;
}
```

### 4.2 Окно

Форма уже существует в `raster-core-panels/types.ts` и меняется минимально:

```ts
export interface WindowDefinition {
  id: string;
  order: number;
  title: { en: string; ru: string };
  icon: string;
  defaultVisible: boolean;
  component: string;
  /** Модальные окна, которые окно может открыть. */
  modals?: readonly string[];
  /** Команды для контекстного меню внутри окна (правый клик по слою и т.п.). */
  contextMenu?: readonly string[];
}
```

### 4.3 Команда

Контракт ядра уже есть. Добавляются три поля — **все три нужны для сценариев**:

```ts
export interface CommandDefinition extends Command {
  /** Куда софт вставляет пункт: меню, контекстное меню слоя, панель, палитра. */
  surfaces: readonly ("menu" | "palette" | "layer-context" | "canvas-context" | "toolbar")[];
  /** Аргументы. Без схемы сценарий нечего записывать и нечем воспроизводить. */
  args?: ArgSchema;
  /** Запрещена к записи в сценарий (сама запись, воспроизведение, undo). */
  neverRecord?: boolean;
}
```

Отмена не отдельное поле: команда обязана выполняться через
`history.execute()` и потому обратима по построению. Команда, которая правит
документ мимо истории, — ошибка, и её ловит контрактный тест (см. 6.3).

### 4.4 Правило

```ts
export interface RuleDefinition {
  id: string;
  order: number;               // порядок применения детерминирован
  applies(edit: PixelEdit, ctx: RuleContext): boolean;
  /** Преобразует правку. Вето — вернуть null. */
  transform(edit: PixelEdit, ctx: RuleContext): PixelEdit | null;
}
```

Первые правила, которые переезжают сюда из разных мест кода:

1. `selection-confines-edits` — из `confineToSelection`
2. `lock-transparent-pixels` — из `paintMask`
3. `lock-pixels` / `lock-position` — сегодня разбросано
4. `layer-kind-guard` — из `RASTER_ONLY_TOOLS`
5. `clipping-mask-bounds`

**Урок, который надо закрепить.** Точка-предохранитель для выделения
существовала, но одна строка (`commitRevision`) дотянулась мимо неё к
необрезанному буферу: экран показывал обрезанное, а redo вернул бы полное.
Починено коммитом `b7c1f84`. Вывод для движка правил: недостаточно иметь
единственное место проверки — до **всех** потребителей (экран, история,
хранилище, экспорт) должен доходить **один и тот же объект**. Это проверяемое
свойство, и на него нужен тест.

### 4.5 Элемент интерфейса

Атомы — с полным поведением, а не «просто input».

- **`NumberBox`** — ввод цифрами; стрелки ↑↓; колесо мыши; перетаскивание вверх-вниз
  с зажатой ЛКМ; `Shift` — крупный шаг, `Alt` — мелкий; принимает единицы
  (`1 см`, `1 px`, `1 пикс`, `10%`) и приводит их по текущим настройкам
  документа; `Esc` — откат к значению до правки.
- **`Slider`** — почти всегда в паре с `NumberBox`, двусторонняя связь; `Alt` —
  точный режим; двойной клик — значение по умолчанию.
- **`Curve`** — интерактивная, точки добавляются/удаляются/тащатся,
  используется коррекциями и Camera Raw.
- **`Histogram`** — интерактивная там, где задаёт уровни.
- **`EyedropperButton`** — временно включает инструмент пипетки и возвращает
  цвет **в вызвавшее окно**, после чего восстанавливает прежний инструмент.
- **`ValueBadge`** — плашка с числом у курсора (размеры при трансформации);
  единица измерения берётся из настроек, а не зашита.

Правило для всех оверлеев: **интерфейс не масштабируется зумом**. Размеры — в
экранных пикселях. Единственное исключение — след инструмента (кольцо кисти),
потому что он показывает реальный отпечаток. Проверяется тестом.

### 4.6 Среда

```ts
export interface EnvironmentManifest {
  kind: string;                 // "raster"
  label: LocalizedText;
  accent: string;               // токен темы, не литерал
  icon: string;
  newDocument: NewDocumentSchema;   // что показывать в окне создания
  documentKindGuard(state: unknown): boolean;
}
```

### 4.7 Плагин

```ts
export interface PluginManifest {
  id: string;
  apiVersion: number;           // отказ при несовпадении, а не падение
  label: LocalizedText;
  environment: string;
  permissions: readonly ("read-document" | "write-pixels" | "network" | "filesystem")[];
  commands?: string;            // подкаталог со своими командами
  entry: string;
}
```

Плагин исполняется **в Worker**, а не в главном потоке: чужой код не должен
подвешивать интерфейс и не должен иметь доступа к DOM. Обмен — сообщениями по
той же схеме `PixelEdit`, что у инструментов, поэтому правила применяются к
плагину автоматически, без отдельной ветки.

---

## 5. Этапы

Каждый заканчивается зелёными тестами и работающим приложением.

### Этап 0. Замки

Перекладка без страховки — это разрушение. Сначала страховка.

- [x] Бенчмарк композита, перемещения, обрезки правки по границам и памяти
      слоёв — `packages/env-raster/src/performance.bench.test.ts`;
      автосохранения — `packages/kernel/src/performance.bench.test.ts`.
      Цифры перемерены на текущем коде, а не взяты из памяти:

      | операция | старая цифра (из памяти) | перемерено сейчас | как задан порог |
      |---|---|---|---|
      | композит 21 слоя 1920×1080 | ~14 мс | **~27 мс** | ×6 от измеренного |
      | перемещение слоя (block-copy) | ~14 мс/кадр | **~0.7 мс** | абсолютный пол 5 мс |
      | обрезка правки по границам | — | **~8 мс** | ×6 от измеренного |
      | автосохранение (1 из 21 буфера) | ~8 мс | **<1 мс** | абсолютный пол 5 мс |
      | память пикселей, 21 слой | ~3.1 МБ | **~13.4 МБ** на этой фикстуре | <30% от «все слои в размер холста» |

      Старая цифра композита (14 мс) не подтвердилась — либо была измерена на
      документе с более компактными слоями, либо ошибочно перенесена из
      контекста предыдущей сессии. **CLAUDE.md и раздел 2 этого документа
      обновлены**: 14 мс убрано как факт, оставлено только со ссылкой на то,
      что оно непроверенное.

      Первая версия фикстуры сама была неправильной: все 21 слой на весь
      холст непрозрачным цветом с дробной прозрачностью — это заставляет
      каждый пиксель идти по самой медленной ветке блендинга на каждом слое
      сразу, и дало бессмысленные ~500 мс. Слои по всему холсту вообще
      противоречат сути самой оптимизации (166→3.1 МБ достигнуты именно
      потому, что реальные слои не занимают весь холст). Исправлено:
      единственный фон на весь холст + 20 слоёв реального размера мазка,
      проведённых через `setLayerPixels`/`trimToContent`, как это делает
      настоящая правка.

      Порог памяти получил аналогичную правку: первая версия требовала
      «меньше 10 МБ» — число, придуманное не глядя на то, что один фоновый
      слой на 1920×1080 уже весит 8.3 МБ. Заменено на структурное сравнение
      (меньше 30% от стоимости «все слои по размеру холста»), которое не
      сломается при следующей правке фикстуры.
- [x] Прогон бенчмарка идёт в общем `pnpm test` — отдельного шага не
      требуется, файлы `*.bench.test.ts` подхватываются тем же `vitest run`
- [~] Снимки экрана эталонных сцен (растр, вектор, панели) — просмотрены
      вживую в этой сессии (стартовый экран, растровая среда с меню/
      инструментами/слоями, векторная среда со своим набором инструментов и
      вкладками документов), визуальных регрессий не обнаружено. **Не
      выполнено полностью**: инструментарий этой сессии не даёт способа
      сохранить скриншот Browser pane как файл на диске — `computer`
      возвращает изображение только в диалог, не в файл. Playwright/puppeteer
      в проекте не установлены, и ставить headless-браузер (десятки–сотни МБ)
      ради одного чекбокса — решение, которое стоит принять осознанно, а не
      походя внутри этапа 0; см. `CLAUDE.md` про непропорциональные
      зависимости. Follow-up записан в разделе 6.3.
- [x] `docs/migration-plan.md` (этот файл) и `CLAUDE.md` в репозитории

### Этап 1. i18n до разноса ✅ выполнено

Делается **первым**, иначе 30 инструментов придётся править дважды.

- [x] `label: LocalizedText` вместо `"Brush Tool (Кисть)"` во всех
      определениях — `tools.ts` (30 инструментов + общие `ToolOption`),
      `commands.ts` (80 команд), `environment.ts` (4 среды). Форма оказалась
      не буквально `{ en, ru }`, как было написано здесь изначально, а
      `{ en: string; ru?: string; uk?: string; es?: string; de?: string;
      ja?: string; zh?: string }` — `en` обязателен, остальные необязательны
      и падают на английский. Причина отклонения от буквального текста
      плана: `docs/requirements.md` (SHELL-007) уже фиксирует именно эту
      политику отказа («untranslated strings use explicit EN fallback») для
      зарезервированных 7 языков, а объект с двумя обязательными полями не
      способен её выразить — пришлось бы либо дублировать английский текст в
      несуществующие переводы, либо снова переделывать формат при первом
      реальном добавлении языка, то есть ровно то «дважды», ради которого
      существует этот этап
- [x] `i18n.ts`: добавлены `LocalizedText`, `resolveLabel` (выбор строки с
      падением на английский) и `legacyBilingualLabel` (сборка строки
      `"English (Русский)"` из структурированных данных для команд ядра —
      `CommandRegistry.search()` ищет по сырому `Command.label`, а команды
      регистрируются один раз при старте, а не читаются заново при каждом
      рендере, как `ToolDefinition` — значит, живое переключение языка и
      поиск на любом языке должны продолжать работать без переизобретения
      контракта `Command` в ядре прямо сейчас; это сознательно отложено до
      этапа 7, когда каталог команд и так переезжает в отдельные файлы).
      Обошлись без отдельного словаря по ключам — не потому что он хуже, а
      потому что тот же fallback-контракт получается из одного поля
      `LocalizedText` дешевле, без схемы именования ключей и без риска
      рассинхронизации ключа с местом использования
- [x] Лint-тест: `apps/web/src/catalogue-i18n.test.ts`, ищет `label:`/
      `category:`/`title:`, за которым следует строковый литерал с
      кириллицей внутри скобок, по трём файлам-каталогам. Проверено, что
      тест реально ловит регрессию (временный откат одной строки → тест
      падает), не просто зелёный по умолчанию
- [x] RU/EN/UK/ES/DE/JA/ZH — уже зарезервированы в `store.ts`'s `Language`
      до этого этапа; `LocalizedText` принимает все семь, требуя реально
      заполненным только `en`

**Побочно найденный и исправленный баг**: `registerToolShortcuts()` строил
подпись команды переключения инструмента как `group.map(tool =>
tool.label).join(" / ")` — то есть склеивал уже двуязычные строки вида
`"Blur Tool (Размытие)"` через `/`, а `localized()`'s regex (`^(.*?)\(([^()]*)\)$`)
матчит только ПОСЛЕДНЮЮ скобочную пару во всей строке. Для группы на букву R
(Blur+Smudge) результат на русском показывал только «Палец», теряя «Размытие»,
а на английском в подпись затесывался обрывок русского текста. Починено как
прямое следствие смены формата (`resolveLabel(tool.label, "en").join(...)` /
`resolveLabel(tool.label, "ru").join(...)` раздельно, затем
`legacyBilingualLabel`) — проверено вживую через `kernel.commands.list()`:
`tool.raster.r` теперь корректно показывает все три инструмента группы на
обоих языках.

**Проверено вживую** (свежий dev-сервер, не унаследованный от предыдущей
сессии — на старом сервере всплыла ложная тревога: `label.match is not a
function`, которая существовала ещё ДО этого этапа, то есть являлась
застоявшимся артефактом HMR за много часов правок, а не регрессией; после
перезапуска сервера на чистом коде ошибки нет ни до, ни после стадии 1):
русская и английская панель инструментов, всплывающая группа инструментов,
командная палитра, живое переключение языка на лету, окно горячих клавиш в
настройках (там подписи команд по-прежнему показывают сырую двуязычную
строку без вызова `localized()` — это существовавшее до этапа 1 поведение,
не регрессия, сохранено байт в байт через `legacyBilingualLabel`).

Типы чистые, 405 тестов зелёных (было 401 на конец этапа 0, +4 новых:
контрактный лint-тест на 4 проверки).

### Этап 2. Атомы интерфейса и тема ✅ выполнено

- [x] **`NumberBox` со всеми способами ввода (4.5) + тесты на каждый** —
      `ui/atoms/NumberBox.tsx` плюс `ui/atoms/number-input.ts`, где лежит вся
      арифметика: ввод текстом с суффиксом единиц (`2cm`, `2 см`, `1 дюйм`,
      `25.4mm`, `72pt`, `30px`) с пересчётом по разрешению документа, стрелки,
      колесо, скраб-перетаскивание, Shift ×10 / Alt ÷10, Enter, Escape,
      запятая как десятичный разделитель. 22 теста.

      **Поведение вынесено в чистый модуль, а не тестируется через DOM**: в
      репозитории нет ни jsdom, ни testing-library, и добавлять их в
      браузерное приложение ради одного поля — непропорционально. Компонент
      остался тонкой обвязкой: событие → чистая функция → `onChange`.

      **Скраб-перетаскивание висит на подписи, а не на самом поле** — как в
      Photoshop. На поле оно дралось бы с выделением текста: тем же жестом
      выделяют цифры, чтобы их перенабрать.
- [x] `Slider`, `Checkbox`, `Select`, `ColorSwatch` — `ui/atoms/`. `Slider`
      использует ту же `applyStep`, что и `NumberBox`, поэтому ползунок и
      число рядом не могут разойтись в том, что такое «один шаг»
- [~] `Button`, `Icon` — **не сделаны намеренно**. Ни один потребитель их
      сейчас не требует, а компонент без места вызова — мёртвый код. `Icon`
      имеет смысл завести, когда каталог инструментов (этапы 3–5) начнёт
      рисовать иконки из определений; тогда у него будет настоящий контракт,
      а не догадка о нём
- [x] `SliderWithNumber`, `OptionRow` — `ui/molecules/`
- [~] `LabeledField` — **создан и удалён в этой же сессии**: ни `OptionRow`,
      ни `SliderWithNumber` в нём не нуждаются (каждый атом рисует свою
      подпись сам), а держать неиспользуемый компонент — та же мёртвая
      абстракция. Вернётся, когда появится потребитель
- [x] `OptionsBar` перестроен на атомы, вид не изменился — проверено вживую
- [x] **Дубликат устранён**: `raster-adjustments/AdjustmentEditor.tsx` имел
      собственные `NumberSlider` и `Check` — вторую реализацию того же ряда
      «ползунок + число». Обе стали обёртками над общими молекулой и атомом,
      так что ~35 мест вызова не тронуты, а диалоги коррекций бесплатно
      получили колесо, перетаскивание, единицы и Escape
- [x] Тест: `apps/web/src/ui/theme/tokens.test.ts` — 4 проверки

**Отклонение от буквального текста пунктов про токены.** `tokens.json` не
заведён, и «ни одного литерального цвета в `styles.css`» не достигается — это
неверная цель, и измерение показало почему. Из 189 литералов примерно половина
**обязана** остаться литеральной: шахматка прозрачности, спектральная полоса
пипетки, цветовые метки слоёв (красная метка красная в любой теме), цвета
уровней важности, и — главное — оверлеи на холсте. Кольцо курсора, нарисованное
краской светлой темы, исчезает на светлом изображении; они и рисуются тёмным
ореолом под светлым пером (см. `CLAUDE.md`, приём взят у Patchy). Перевод их в
токены был бы регрессией.

Токены при этом уже жили в CSS как переменные с переопределениями
`[data-theme]` — это рабочая и идиоматичная схема, и перекладывать их в JSON с
инлайновой инъекцией значило бы бороться с каскадом ради формы. Поэтому
сделана полезная половина: **тест** проверяет, что (1) каждый читаемый
`var(--x)` где-то объявлен, (2) тема не вводит токенов, которых нет в базе,
(3) хром интерфейса не содержит литеральных цветов, а каждое исключение
перечислено с причиной.

**Найденный и исправленный баг светлой темы.** Диалоги коррекций и стилей слоя
были захардкожены тёмными (`#4a4a4a`, `#464646`, `#ddd`) и оставались тёмными
при светлой и контрастной теме — видимый дефект, ради которого работа с
токенами вообще имела смысл. Переведены на токены, проверено во всех трёх темах.

**Ещё три дефекта, вскрытых тестом на определённость токенов:**

- `--panel`, `--hover`, `--progress` читались, но нигде не объявлялись.
  Неопределённая переменная делает **всё объявление** невалидным, поэтому у
  меню коррекций не было фона при наведении, а у клипа на таймлайне —
  позиции плейхеда вообще. Объявлены значения по умолчанию.
- `--accent-ink` (текст на акцентной заливке) тоже не был объявлен, и его
  литеральный fallback был несущим. При этом на одном и том же голубом
  `--focus` одни кнопки рисовали тёмный текст, другие — белый; белый на
  светло-голубом был просто ошибкой. Приведено к одному токену.
  Я сначала завёл под это имя `--on-accent` и переименовал, обнаружив, что
  имя `--accent-ink` в коде уже было.

Типы чистые, 431 тест зелёный (было 405 на конец этапа 1: +22 на ввод чисел,
+4 на токены).

### Этап 3. Каркас каталога инструментов ✅ выполнено

- [x] `ToolContext` и `RasterToolDefinition` —
      `environments/raster/tools/types.ts`. Хуки взяты из таблицы §4.1, то
      есть выведены из реальных ветвлений, а не придуманы: `onPointerDown`,
      `onPointerMove`, `onGestureEnd`, плюс `Overlay` для того, что
      инструмент рисует поверх холста
- [x] `environments/raster/tools/registry.ts` на `import.meta.glob`, шаблон
      `definitions/*.{tsx,ts}` — тот же, что у каталога коррекций
- [x] Мост в `RasterWorkspace`: инструмент из каталога идёт по хукам и до
      `switch` не доходит, остальные — как раньше. Оба пути живы
- [x] Перенесён `raster.eyedropper` — старая реализация удалена целиком
      (две реализации одного инструмента держать нельзя)
- [x] Проверено вживую: цвет берётся точно (`#1ec83c` на полосе
      `rgb(30,200,60)`), лупа появляется при нажатии и исчезает при
      отпускании, её увеличенный холст 17×17 корректно показывает границу
      полос, обводка лупы окрашена взятым цветом, hex-подпись верна;
      усреднение 5×5 на границе даёт смешанный цвет; `allLayers: true` даёт
      композит, `false` — активный слой. Старый путь не задет: кисть рисует
      и откатывается, выделитель строит выделение

**Решения, принятые при проектировании контракта:**

- **Состояние инструмента держит мастерская, а не файл инструмента.** Один
  слот на `id`, передаётся в хуки через `ctx.state` / `ctx.setState`. Так файл
  инструмента остаётся обычным объектом без собственных хуков, а смена
  инструмента не может оставить недоделанный жест работать.
- **`Overlay` — компонент, а не императивная ручка.** То, что инструмент
  рисует, есть функция от его состояния, и React уже умеет держать эти двое в
  согласии. Размеры внутри — экранные пиксели, по правилу «интерфейс не
  зумируется».
- **Описательная половина инструмента (подпись, иконка, шорткат, схема
  настроек) пока осталась в `tools.ts`.** Панель инструментов и панель
  настроек читают её оттуда, и оба пути обязаны работать, пока инструменты
  переезжают по одному. Слияние — задача этапа 5.
- **`ctx.commit` заведён, но ни разу не вызван.** Пипетка читает, а не пишет.
  Он подключён к существующему `commitPixels`, то есть не выдуман, но это
  самая слабо подтверждённая часть контракта до этапа 5.
- **`ToolContext` растёт по требованию.** В нём ровно то, что понадобилось
  пипетке (`layerPixels`, `compositePixels`, `capturePointer`,
  `setForegroundColor`, `options`, `state`), а не то, что может пригодиться.
  Контекст, спроектированный вперёд своих потребителей, — догадка, а ему ещё
  переживать двадцать девять инструментов.

**Побочно найденный и исправленный дефект.** Опция «Показывать лупу» была
объявлена у волшебной палочки, где её никто не читает, а пипетка её читала,
не имея — то есть галочка ничего не делала, а альтернатива в виде чипса была
недостижима. Опция перенесена на свой инструмент; проверено, что со снятой
галочкой появляется чипс цвета `#3366cc` вместо лупы.

**Метрики.** `RasterWorkspace.tsx` 2068 → 2062 строки, ветвлений по
`activeToolId` 49 → 48. Почти без изменений — и так и должно быть на первом
инструменте: мост добавил примерно столько же, сколько убрала пипетка. Выигрыш
начнётся с этапа 5, когда мост уже написан, а инструменты уходят один за
другим.

431 тест зелёный, типы чистые.

### Этап 4. Контрактный тест каталога ✅ выполнено

Пишется до массового переноса — он и есть страховка для остальных 29.

`environments/raster/tools/contract.test.ts` прогоняет **каждый** инструмент
каталога через жест «нажал — провёл — отпустил» на реальном документе и
проверяет:

- [x] **не пишет пиксели мимо `ctx.commit`** — инструменту выдаётся копия
      документа, которая после жеста сверяется с нетронутым оригиналом.
      Инструмент, залезший в `context.document` или удержавший буфер из
      `layerPixels()`, виден только здесь
- [x] **правка обратима** — у каждого коммита непустая подпись, две стороны
      не один и тот же объект (иначе история восстанавливала бы ровно то
      состояние, которое отменяет), длины совпадают и соответствуют либо
      холсту, либо границам слоя
- [x] **`onDeactivate` очищает состояние** — прогоняется случай «сменил
      инструмент, не отпустив кнопку», после чего состояние обязано
      совпасть с `createState()`
- [x] **каждая настройка влияет на результат** — инструмент прогоняется с
      каждой опцией, сдвинутой с умолчания, и подпись результата обязана
      измениться. Это то самое правило «если у инструмента есть настройка,
      значит она на него влияет»
- [x] **описан в `tools.ts`** — запись каталога без описания это инструмент,
      который нельзя выбрать
- [x] Тест падает, если каталог пуст (иначе весь набор зеленел бы от того,
      что glob ничего не нашёл)

**Проверки не вакуумные — это подтверждено, а не заявлено.** Пипетка была
сломана тремя способами по очереди, и каждый раз падала нужная проверка с
внятным сообщением: убрал `onDeactivate` → «returns to its initial state»;
заставил игнорировать `allLayers` → «option "allLayers" changed nothing»;
заставил писать в документ напрямую → «writes no pixels except through
commit». После восстановления — снова 10 из 10. Плюс в файле лежат три
теста на сами проверки, с нарочно испорченными игрушечными инструментами.

**Оговорка, которую важно не потерять.** В каталоге пока один инструмент, и
он **читает**, а не пишет. Поэтому проверки о содержимом коммитов сегодня
ни на чём не срабатывают — они написаны так, чтобы громко упасть, когда на
этапе 5 приедет рисующий инструмент и сделает это неправильно. Это
единственная часть набора без сегодняшнего подтверждения.

**Про «уважает выделение».** Проверять это у инструмента нечего: правило
живёт в `commitPixels`, куда ведёт `ctx.commit`, и обойти его можно только
записью в документ напрямую — что ловит первая проверка. В тесте это
записано явно, вместо проверки, которая не может упасть.

**Про «прицел не масштабируется зумом».** Не проверяется здесь и намеренно:
оверлей — React-компонент, его размеры в CSS, и без DOM-окружения (которого
в проекте нет, см. этап 2) утверждение о нём было бы имитацией. Правило
держится тестом токенов и разбором в `CLAUDE.md`; когда появится
DOM-окружение, проверка ляжет сюда.

**Найденный и исправленный дефект.** `onDeactivate` в контракте не было
вовсе. Состояние инструмента живёт по его `id` и переживает смену
инструмента — значит, если сменить инструмент, не отпустив кнопку, лупа
пипетки оставалась в состоянии и рисовалась заново при возвращении к
инструменту. Воспроизведено вживую (лупа возвращалась), добавлен хук, вызов
в мосте и проверка в контрактном тесте; после исправления не возвращается.

441 тест зелёный, типы чистые.

### Этап 5. Перенос инструментов

Порядок — от простого к сложному, чтобы контракт проверялся на растущей
нагрузке и уточнялся до того, как станет дорого.

- [ ] Навигация: `hand`, `zoom`, `rotateView`
- [ ] Выделение: `marquee`, `ellipseMarquee`, `lasso`, `magicWand`
- [ ] Рисование: `brush`, `pencil`, `highlighter`, `eraser`
- [ ] Тональные: `blur`, `smudge`, `dodge`, `burn`
- [ ] Заливка и текст: `fill`, `text`, `shape`
- [ ] Ретушь: `clone`, `spotHeal`, `patch`
- [ ] Сложные: `move` (авто-выбор, трансформация), `crop`
- [ ] Вектор: 6 инструментов
- [ ] Старый `switch` и мост удалены
- [ ] `RasterWorkspace.tsx` ≤ 400 строк

### Этап 6. Движок правил

- [ ] `rules/engine.ts`, применение по `order`, детерминированно
- [ ] `ctx.commit` — единственный путь к пикселям
- [ ] Пять правил из 4.4 перенесены
- [ ] Тест: объект, дошедший до экрана, тождественен дошедшему до хранилища
- [ ] Тест: инструмент, не знающий о выделении, всё равно ему подчиняется

### Этап 7. Окна, команды, модальные окна

- [ ] `windows/` внутри каждой среды; `raster-core-panels` переезжает
- [ ] Список «Окна» с галочками, свой на среду
- [ ] `commands/definitions/` по средам; все 80 команд разложены
- [ ] Поля `surfaces`, `args`, `neverRecord`
- [ ] `modals/` с реестром; создание документа, ошибки, прогресс, подтверждение
- [ ] Модальное окно вызывается по id — ссылка, а не импорт

### Этап 8. Панель инструментов

Первое видимое пользователю изменение.

- [ ] Перетаскивание инструментов
- [ ] Кнопка `(...)`: показать/скрыть, группировка
- [ ] Раскладка панели переживает перезагрузку, своя на среду
- [ ] Сброс к раскладке по умолчанию

### Этап 9. Сценарии

- [ ] Запись команд с аргументами
- [ ] Чёрный список: `neverRecord` + всё из `script.*` и `edit.undo/redo`
- [ ] Воспроизведение с остановкой на ошибке
- [ ] Окно «Сценарии» в каталоге окон
- [ ] Команды `script.record`, `script.stop`, `script.play`, `script.delete`
- [ ] Тест: запись сценария не записывает саму себя

### Этап 10. Кисти, паттерны, нейросети, плагины

- [ ] `assets-library/brushes/`, `patterns/` с реестрами
- [ ] `ml/` — по файлу на задачу, ленивая загрузка сохранена
- [ ] Плагины в Worker, `apiVersion`, права
- [ ] Плагин-образец в репозитории как живая документация

### Этап 11. Платформа и запуск

- [ ] `platform/` — интерфейс: файлы, темпы, бэкапы, шрифты, буфер обмена
- [ ] Реализация `web` (OPFS/IndexedDB) и `tauri` (ФС)
- [ ] Настройка места хранения темпов и бэкапов (только не-веб)
- [ ] Окно загрузки: инициализация каталогов, чтение системных шрифтов,
      прогресс с именами этапов
- [ ] Инвентаризация каталогов на старте кэшируется, а не пересчитывается

### Этап 12. Десктоп

Цель: **самостоятельное окно-приложение**, `.exe` на Windows, `.app` на macOS.

- [ ] Tauri v2 подключён; тот же код собирается в веб и десктоп
- [ ] Windows: `.exe` + `.msi`, ярлык, ассоциация с форматом документа
- [ ] macOS: `.app` + `.dmg`, Universal (Apple Silicon + Intel)
- [ ] macOS: подпись и нотаризация — **без них приложение не запустится у
      пользователя**, Gatekeeper заблокирует; нужен Apple Developer ID
- [ ] Windows: подпись — иначе SmartScreen показывает предупреждение
- [ ] Нативное меню вместо HTML-меню на десктопе
- [ ] Веб-сборка на GitHub Pages
- [ ] **Ограничение Pages зафиксировано**: заголовки COOP/COEP там не задать,
      поэтому `SharedArrayBuffer` недоступен и многопоточный WASM ограничен.
      Обход — service-worker-шим; решение принять явно и записать
- [ ] Сравнительный замер веб/десктоп, разница задокументирована

---

## 6. Сквозные задачи

### 6.1 Формат документа

- [ ] Версионированный контейнер `.vravio`
- [ ] Правило миграции: читаем на версию назад, пишем текущую
- [ ] Тест: документ прошлой версии открывается

### 6.2 Потоки

- [ ] Решено и записано: кто владеет пикселями, что уходит в Worker
- [ ] `OffscreenCanvas` там, где выигрывает
- [ ] Плагины и тяжёлые фильтры — только Worker

### 6.3 Тесты

- [ ] Контрактный тест на инструменты (этап 4)
- [ ] Аналогичный на окна, команды, коррекции, плагины
- [ ] Тест «команда правит документ только через историю»
- [ ] Тест «оверлей не масштабируется зумом»
- [x] Бенчмарки написаны и подхватываются `pnpm test`, порог из этапа 0
- [ ] Персистентные снимки эталонных сцен для сверки между сессиями: нужен
      лёгкий способ сохранить скриншот Browser pane как файл, которого
      сегодня нет (см. этап 0). Решение — не Playwright целиком (тяжёлая
      зависимость на весь проект), а точечный скрипт на `puppeteer-core` c
      уже установленным Chrome/Chromium системы (`/Applications/Google
      Chrome.app` есть на этой машине) — тонкая обёртка, не встраивается в
      сборку. Оценить при следующей необходимости в визуальном diff, не
      добавлять заранее «на будущее»

### 6.4 Долги, которые чинятся заодно

- [ ] `camera-raw-filter.ts` (299 строк) — тестов нет
- [ ] `psd-import.ts` (294) — тестов нет; добавить проверку границ курсора
- [ ] `scene3d-render.ts` (120) — тестов нет
- [ ] Круговой обмен **вектор↔растр** не покрыт: в `roundtrip.test.ts` слово
      `vector` не встречается, хотя коммит `3581d66` его обещает
- [ ] Штамп: `cloneSourceView` — React-состояние на каждом `pointermove`;
      предпросмотр масштабируется зумом
- [ ] `docs/requirements.md`: из ~30 строк `verified` только у двух

### 6.5 Управление цветом

- [ ] Решено, где живёт цвет, до разноса операций по файлам
- [ ] Место под 16/32 бит и CMYK заложено (в `requirements.md` они обязательны)

---

## 7. Риски

| риск | как снимаем |
|---|---|
| Большой разрыв ломает всё | Мост на этапе 3: старый и новый путь живут вместе; переносим по одному |
| Атомарность съедает производительность | Граница из раздела 2; бенчмарк-порог с этапа 0 |
| Контракт инструмента оказался неполным | Он выведен из 49 реальных ветвлений, а не придуман; уточняется на трёх инструментах до массового переноса |
| Правило можно обойти | `ctx.commit` — единственная дверь; тест на тождество объекта |
| i18n переделывать дважды | Этап 1 идёт до разноса |
| macOS не запустит сборку | Подпись и нотаризация вписаны в этап 12, а не оставлены на потом |
| Плагин вешает интерфейс | Worker, не главный поток |

---

## 8. Что считается успехом

1. Новый инструмент — **один файл** в `environments/<среда>/tools/definitions/`.
   Ни одной правки в другом месте.
2. Новое окно, команда, правило, коррекция, плагин — то же самое.
3. Новая среда — **один подкаталог** с `environment.ts`.
4. `RasterWorkspace.tsx` ≤ 400 строк; ни одного `activeToolId === "..."`.
5. Ни одна цифра из раздела 1 не ухудшилась.
6. Приложение открывается в браузере, `.exe` на Windows и `.app` на macOS из
   одного кода.
