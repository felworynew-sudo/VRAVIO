# ТЗ: векторная часть VRAVIO

Документ описывает, как из нынешнего векторного редактора — шести инструментов
поверх плоского списка фигур — сделать среду уровня Illustrator, не сломав того,
что уже работает, и не заведя вторую модель документа рядом с ядром.

Язык документа — русский, идентификаторы и имена файлов — английские, как во
всём репозитории. Правила работы — те же, что в `CLAUDE.md`: расширение файлом,
а не веткой; единственная дверь; объявленная опция обязана влиять на результат;
каждая проверка доказана падающей, когда ломаешь то, что она стережёт.

**Как пользоваться.** Каждый этап заканчивается состоянием, в котором проект
собирается, проходит тесты и работает. Между этапами можно остановиться на любой
срок. Отмечайте выполненное прямо здесь: `- [x]`. Строка закрыта, только когда
есть и реализация, и автопроверка.

---

## 1. Инвентарь на момент составления

Цифры получены измерением репозитория 5 сентября 2026 года, а не оценкой.

| | значение |
|---|---|
| Весь векторный код (TS/TSX) | **1 211 строк** |
| `apps/web/src/VectorWorkspace.tsx` | 308 |
| `apps/web/src/vector-environment.ts` | 185 |
| `apps/web/src/vector-commands.ts` | 83 |
| `packages/env-vector/src/shape-ops.ts` | 82 |
| `packages/env-vector/src/types.ts` | 68 |
| `packages/env-vector/src/path-data.ts` | 21 |
| Инструментов | **6** (выделение, узлы, перо, прямоугольник, эллипс, текст) |
| Видов фигур | **6** (rectangle, ellipse, line, path, text, image) |
| Тестов | **36** |

Для сравнения: растровая среда — 24 инструмента, десятки фильтров, PSD-импорт,
3D-слои, RAW-декодер. Вектор отстаёт не на функцию, а на порядок.

### Что уже устроено правильно

Это фундамент, его не переделываем, а достраиваем.

- **Инструменты — каталог файлов** (`environments/vector/tools/definitions/`),
  тот же контракт, что у растровых, с контрактным тестом на 241 строку.
- **Окна — каталог файлов**, пять панелей.
- **Круговой обмен с растром работает и проверен**: изображение в векторном
  документе хранится как `pixelAssetId` — ссылка на ассет ядра, а не копия
  пикселей. Открыть картинку в растре, поправить и увидеть изменение здесь —
  единственная строка в `docs/requirements.md`, помеченная `verified`
  (ROUNDTRIP-VECTOR-001).
- **Документ живёт вне React**, история и ассеты — общие с растром.
- **`pathData`** уже отдаёт строку, пригодную и для `<path d>`, и для `Path2D`.
  Это готовая точка стыка с любым будущим рендером.

---

## 2. Что сломано или отсутствует. Измерено, а не предположено

### 2.1. Габарит пути считается неверно — подтверждено прогоном

`shapeBounds` для пути делает `Math.min(...xs, 0)`. Ноль в списке означает, что
габарит **всегда растягивается до начала координат**.

Проверено: путь 100×100, нарисованный в точке (500, 500), отдаёт габарит
`{x: 0, y: 0, width: 600, height: 600}`. То есть рамка выделения в шесть раз
больше фигуры, а попадание курсором срабатывает по всему левому верхнему углу
документа.

Вторая ошибка там же: габарит считается по опорным точкам и **игнорирует ручки
Безье**. Кривая, выпирающая за свои опорные точки, обрезается.

### 2.2. Попадание курсором — по прямоугольнику габарита

`hitTestShape` проверяет только прямоугольник. Клик в дырку буквы «О» выделяет
её; клик далеко от тонкой диагональной линии выделяет её же. Настоящего
попадания по заливке и по обводке нет.

### 2.3. Габарит текста выдуман

`width: Math.max(40, value.length * fontSize * .55)`. Это не измерение, а
формула. При этом настоящее измерение в проекте **уже есть** — `textRender.ts`
пользуется `measureText` для растрового текста. Векторный текст просто не
подключён к нему.

### 2.4. Нет трансформации как таковой

У прямоугольника и эллипса есть `rotation`, у пути и текста нет ничего. Нет
матрицы, нет наследования преобразования, нет масштабирования группы.

### 2.5. Нет групп и вложенности

`shapes` — плоский массив. Ни группы, ни слоя, ни артборда как контейнера.

### 2.6. Нет булевых операций, офсета, упрощения, обводки в контур

Ни объединения, ни вычитания, ни пересечения, ни «преобразовать обводку в
объект».

### 2.7. Нет градиентов, паттернов, множественных заливок и обводок

`VectorStyle` — это `fill: string | null`, `stroke`, `strokeWidth`, `opacity`.
Ни градиента, ни пунктира, ни концов линии, ни соединений, ни режимов наложения.

### 2.8. Нет привязок

Ни к сетке, ни к направляющим, ни к узлам, ни к пересечениям, ни к центрам.

### 2.9. Поиск фигуры под курсором — линейный

`shapeAt` перебирает весь массив на каждое движение мыши. Пространственного
индекса нет.

### 2.10. Рендер — по DOM-узлу на фигуру

`VectorWorkspace` рисует `<svg>` с React-элементом на каждую фигуру. Это
правильный первый выбор — браузер отлично умеет SVG, — но у него есть потолок:
несколько тысяч узлов, и панорамирование начинает заикаться. Потолок надо
**измерить**, а не угадать (этап 1).

### 2.11. Нет импорта и экспорта SVG

Единственное, что умеет File > Open с `.svg`, — растеризовать его как картинку.
Векторный SVG внутрь документа не попадает, и обратно документ не выгружается.

### 2.12. Нет недеструктивности

Нарисовал прямоугольник, скруглил углы, применил офсет — и остался безымянный
путь из сорока семи точек. Исходный прямоугольник исчез.

---

## 3. Решения архитектуры

### 3.1. Недеструктивность — не функция, а форма объекта

Ключевое решение всего документа. Объект хранит **источник** и **стопку
модификаторов**, а не результат:

```
VectorEntity {
  id, name, visible, locked
  source:      Rectangle | Ellipse | Path | Text | Image | Group
  transform:   Matrix
  geometry:    GeometryModifier[]   // офсет, скругление, упрощение, булева
  appearance:  Appearance[]         // заливки, обводки, тени — стопкой
  clip:        ClipSource | null
}
```

Прямоугольник со скруглением 20, офсетом +10 и тремя обводками остаётся
**прямоугольником**: ширину, высоту и радиус можно править после всего.

Отсюда техническое требование: результат вычисления стопки кешируется, а не
пересчитывается на каждый кадр, и инвалидируется по номеру ревизии — тот же
приём, которым уже пользуются ассеты ядра.

**Каждый модификатор — файл в каталоге** (`geometry/definitions/offset.ts`), как
инструменты и команды. Добавление модификатора не должно править ни одной
существующей строки — правило 6 из `CLAUDE.md`.

### 3.2. Граница TypeScript / Rust — не сразу, но спроектированная заранее

Предложение вынести геометрию в Rust/WASM правильное, но **не первым шагом**.

Сейчас векторного кода 1 211 строк, и он не описывает даже групп. Начать с
переноса математики в Rust — значит закрепить в более сложном языке модель,
которая ещё не сложилась, и обзавестись WASM-сборкой раньше, чем появится, что
в неё класть. При этом семантические дыры из раздела 2 — группы, трансформации,
настоящие метрики текста — это **не математика**, их Rust не чинит.

Порядок такой:

1. Этапы 2–6 чинят семантику и делают её измеримой — на TypeScript.
2. Этап 7 вводит **один порт** `VectorGeometryPort` с реализацией на TS
   (медленной, но эталонной) и на WASM (быстрой). Одна дверь, две реализации,
   один набор тестов на обе — тогда «WASM даёт то же самое» становится
   утверждением, которое проверяется, а не надеждой.

Rust-инструментарий на машине уже стоит — его привёз этап 12 основного плана
(Tauri). Цена входа в WASM для этого проекта уже уплачена.

Граница проходит так: **TypeScript — интерфейс, инструменты и смысл документа;
WASM — тяжёлая геометрия без состояния**. Порт принимает и отдаёт плоские
массивы чисел, а не объекты документа. Отсутствие состояния — и есть то условие,
при котором вторая реализация не разъедется с первой.

### 3.3. Рендер за абстракцией, выбор бэкенда по измерению

```
interface VectorRenderer { render(scene, viewport): void }
```

- **SVG-DOM** — нынешний. Остаётся: даёт бесплатно сглаживание, текст,
  доступность и отладку через инспектор браузера.
- **Canvas 2D** — второй, через `Path2D` из уже существующего `pathData`.
- **GPU** — третий, и только если замеры покажут, что нужен.

Переключение — по измеренному порогу числа объектов, а не по вкусу. Порог
попадает в этот документ цифрой после этапа 1.

### 3.4. Модель документа остаётся своей

Ни Paper.js, ни Graphite не становятся хозяином документа. У VRAVIO уже есть
`Kernel`, `Document`, `Assets`, `History`, круговой обмен, и вторая модель рядом
означала бы вечную синхронизацию двух деревьев. Доноры поставляют **алгоритмы и
решения**, а не владение состоянием.

### 3.5. SVG — формат обмена, а не внутреннее представление

```
SVG → импорт → семантическая модель VRAVIO → экспорт → SVG
```

Внутренняя модель богаче SVG (недеструктивные модификаторы, стопки оформления),
и попытка сделать документ SVG-деревом сразу отрезала бы всё из раздела 3.1.

### 3.6. Совместимость документов

`schemaVersion` растёт 2 → 3, миграция пишется вместе с изменением модели и
проверяется тестом на документе прошлой версии. Ни один сохранённый документ не
должен перестать открываться.

---

## 4. Доноры: что берём и на каких условиях

Сведения проверены по репозиториям 5 сентября 2026 года.

| Проект | Лицензия | Что берём | Как |
|---|---|---|---|
| **Graphite** | Apache 2.0 | Архитектуру недеструктивного ядра, разделение `document`/`editor`/`node-graph`/`tools`, машины состояний инструментов | Эталон и источник кода, **не зависимость** |
| **Kurbo** | MIT / Apache 2.0 | Кривые: экстремумы, габариты с учётом ручек, длина дуги, ближайшая точка, пересечения, спрямление, аппроксимация обводки | Крейт Rust (этап 7+) |
| **Lyon** | MIT / Apache 2.0 / MPL 2.0 | Тесселяцию путей в треугольники | Зависимость, только если дойдём до GPU |
| **Clipper2** | Boost 1.0 | Булевы операции и офсет полигонов | Через WASM-сборку либо Rust-порт |
| **resvg / usvg** | MIT / Apache 2.0 | Разбор SVG; ~1600 регрессионных тестов как эталон | `usvg` — парсер, `resvg` — сверка экспорта |
| **Parley + HarfRust** | MIT / Apache 2.0 | Раскладку и шейпинг текста: bidi, лигатуры, переносы, попадание курсором | Зависимость (этап 11) |
| **Vello** | MIT / Apache 2.0 | Возможный GPU-бэкенд | Только по результатам замера |
| **RBush** | MIT | R-дерево для поиска по области | npm-зависимость, сразу (этап 4) |
| **Paper.js** | MIT | Алгоритмы попадания по кривой, пересечений, геометрии выделения | Читаем, переносим точечно |
| **Inkscape** | GPL | **Перечень возможностей**: узловой инструмент, привязки, Live Path Effects, Shape Builder, маркеры, клоны, текст по контуру, трассировка | Энциклопедия того, *что* делать |
| **Krita** | GPL | **Перечень жестов** инструмента «Перо» (Bezier Curve Tool): превью следующего сегмента, `Ctrl`/`Alt`/`Shift`-модификаторы ручек, автосглаживание, привязка по углу | Энциклопедия жестов, тот же режим что и Inkscape — не код |
| **Penpot, Figma, Illustrator** | — | Поведение привязок и выделения; у Illustrator отдельно — модель Canvas/Artboards (этап 15): артборд как прямоугольник-метаданные на общем canvas, не владелец объектов | Эталон ощущения; артборды — почти буквальный повтор архитектуры |

Три предупреждения, которые стоило записать до начала работы:

- **Clipper2 работает с полигонами, не с кривыми** — это сказано в его же
  описании. Значит цепочка `Безье → спрямление → Clipper → полигоны → обратная
  аппроксимация в кривые`. Первое надёжное решение это даёт; результата уровня
  Illustrator, где после объединения остаются гладкие кривые с малым числом
  узлов, — ещё нет. Обратная аппроксимация — отдельная задача этапа 8.
- **У Lyon нет встроенного сглаживания**, это прямо написано в его FAQ. Если
  дойдём до GPU, сглаживание — наша забота.
- **Vello его авторы называют экспериментальным**, а поддержка браузера через
  WebGPU в его описании не заявлена. Поэтому он в плане стоит последним и с
  правом не делать этот этап вовсе.
- Код Inkscape — GPL. Пользоваться им как справочником о том, какие функции
  нужны, — одно; переносить оттуда код дословно — другое, это меняет лицензию
  VRAVIO. В плане он значится только как перечень возможностей.

---

## 5. Этапы

### Этап 1. Измерить нынешнее и застолбить пол — сделано 5 сентября 2026

Цель: цифры, ниже которых нельзя опускаться, и подтверждённый потолок рендера.

- [x] Замер: сколько объектов держит нынешний SVG-рендер при плавном
      панорамировании — искать порог, где кадр превышает 16 мс
- [x] Замер: время `shapeAt` на 100 / 1 000 / 10 000 фигур
- [x] Замер: время открытия и автосохранения документа на 1 000 фигур
- [x] Тест-пол `packages/env-vector/src/performance.bench.test.ts` по образцу
      растрового, с той же оговоркой про нагрузку машины из `CLAUDE.md`
- [x] Тест, фиксирующий баг 2.1 как **ожидаемо неверный** результат, чтобы этап
      3 сломал именно его — `vector.test.ts`, «path bounds stretch to the
      origin»

**Метод — тот же урок, что уже был записан в `CLAUDE.md` про измерение через
`requestAnimationFrame`.** Первая попытка мерить кадры при панорамировании
считала простые кадры между вызовами инструмента как «медленные» — 539 из
1219 кадров оказывались дольше 16 мс просто потому, что между отдельными
вызовами `computer` проходит сетевая пауза, а `requestAnimationFrame`
исправно тикает и на простое. Рабочий метод: несколько тиков скролла в одном
`browser_batch` (без пауз между действиями внутри батча) и сравнение
**худших** кадров, а не среднего по всем.

**Измерено на живом dev-сервере, документ 1920×1080, реальным колесом мыши
(не синтетическим событием) через `computer`:**

| Фигур на холсте | Худшие кадры жеста (мс) | Устойчивое движение |
|---|---|---|
| 3 000 | 157.9 · 137.7 · 64.2 · 63.8 | ~16.5 мс/кадр |
| 10 000 | 283.1 · 277.0 · 172.2 · 128.6 · 128.0 · 125.2 · 121.2 · 107.2 | ~16.5 мс/кадр |

Вывод, а не догадка: **сам жест не заикается на всём протяжении** — как
только панорама начала двигаться, кадр стабильно укладывается в 16.5 мс
независимо от числа фигур. Дорог только **первый кадр смены направления**:
на 3 000 фигурах это 1–2 кадра дороже 60 ms, на 10 000 — уже 6–8 кадров дороже
100 мс, вплоть до 283 мс. Похоже на пересчёт стилей всего поддерева `<svg>`
при смене CSS-трансформации на общем контейнере — вложенность, а не число
кадров в движении, растёт с количеством фигур.

Порог для этапа 12 (когда решать вопрос о GPU-рендере): **если после этапов
2–9 в реальных документах регулярно оказывается 5 000+ фигур, обязательно
подтвердить, ушёл ли стартовый хитч вместе с остальными изменениями, прежде
чем откладывать GPU-рендер дальше**. Ниже 3 000 фигур текущий SVG-рендер не
создаёт заметной проблемы.

Замер `shapeAt` (линейный перебор, точка гарантированного промаха):

| Фигур | Время (fastest of 5) |
|---|---|
| 100 | ~0.008 мс |
| 1 000 | ~0.008 мс |
| 10 000 | ~0.09 мс |

На этих размерах линейный перебор пока не создаёт проблемы — числа сюда
занесены как пол для этапа 4, а не как повод его торопить.

Замер сериализации документа (`JSON.stringify`/`parse` состояния — то, что
стоит на пути автосохранения и восстановления сессии сверх постоянной
стоимости самого ввода-вывода): **~2.2 мс на 1 000 фигур**.

### Этап 2. Модель: сущность, трансформация, группы — сделано 5 сентября 2026

Цель: документ, в котором есть иерархия и настоящее преобразование.

- [x] `transform: Matrix` на каждой фигуре вместо `VectorEntity` с отдельным
      `source`/`appearance` — решение объяснено ниже; `schemaVersion: 3`
- [x] Матрица 2×3 (`matrix.ts`), наследование от родителя к потомку
      (`worldTransform` в `tree.ts`), 9 тестов на композицию/инверсию/поворот
- [x] Группа как вид фигуры (`kind: "group"`); вложенность любой глубины —
      `parentId`/`orderKey`, тот же плоский паттерн, что у `RasterLayer`, а не
      вторая, вложенная модель дерева
- [x] Миграция 2 → 3 и тест на документе версии 2 (`migration.test.ts`, 10
      тестов, включая доказанно неверный результат при сломанной миграции)
- [x] Панель слоёв показывает дерево — `vectorShapeRows`, отступы, треугольник
      сворачивания, многовыбор Shift/Ctrl
- [x] Живая проверка: сгруппировать, повернуть группу, разгруппировать —
      подтверждено на живом dev-сервере реальными кликами; экранный
      `getBoundingClientRect()` прямоугольника до и после разгруппировки
      совпал **побитово** (см. ниже)

**Ловушка, известная заранее, встретилась и была обезврежена тестом
заранее.** Тест «ungrouping an empty active group does not leave
activeShapeId pointing at nothing» написан **до** реализации `ungroupShapes`
и ловит именно тот баг, что уронил растровую разгруппировку. Разгруппировка
пустой активной группы (её и создаёт кнопка «Новая группа» в панели) больше
не оставляет `activeShapeId` висящим на удалённом элементе.

#### Решение: `transform` на фигуре, а не отдельная сущность-обёртка

Раздел 3.1 предполагал `VectorEntity { source, transform, geometry[],
appearance[] }` — источник и стопка модификаторов отдельно от фигуры. На
практике для этого этапа это было бы преждевременно: недеструктивные
модификаторы — это этап 9, а вводить обёртку вокруг каждой фигуры сейчас
означало бы переписать весь код, который держит `VectorShape` как плоскую
дискриминированную структуру, ради поля, которое пока нечем заполнять.

Сделано минимально необходимое для этого этапа: `transform: Matrix` добавлен
прямо в `VectorShapeBase` — то общее для всех видов фигур, что раньше было
только `rotation: number` у прямоугольника, эллипса и картинки (и отсутствовало
у пути, текста и линии — баг §2.4). Это готовит почву под `VectorEntity`
позже, не создавая сейчас двух конкурирующих представлений трансформации.

**`rotation` удалён, а не оставлен рядом.** Единственная дверь: если бы
`rotation` и `transform` существовали одновременно, они могли бы разойтись
между собой, и непонятно, чему верить при рендере. Миграция запекает старое
значение поворота в матрицу вокруг центра фигуры — числа сверены программно
(`/d/DiskD/Temp/check-rotation.mjs`), а не выведены на бумаге.

#### Как рендерится трансформация — переиспользован сам SVG

`VectorWorkspace.tsx` был плоским `state.shapes.map(renderShape)`. Стал
рекурсивным обходом дерева (`renderShapeTree`), где группа — это `<g
transform="matrix(...)">`, обёрнутый вокруг своих детей. Композицию
трансформаций всей цепочки предков делает сам браузер через вложенность
`<g>` — ни `renderShapeTree`, ни `renderShape` не перемножают матрицы
вручную. Матрицы нужны только там, где нет DOM: `shapeAt` (клик мимо фигуры
внутри повёрнутой группы должен промахнуться) и `shapeWorldBounds`
(экранная рамка выделения).

Канвас-рендер (`vector-environment.ts`, экспорт в растр) вложенности не
имеет — это плоский цикл по `state.shapes` — поэтому там трансформация
применяется явно, `context.transform(...)` с посчитанным `worldTransform`
на каждую фигуру. Раньше этот путь вообще не учитывал `rotation`; теперь он
не только компилируется, но и **правильнее**, чем был.

#### Живая проверка, с числами

Создан документ, прямоугольник и эллипс нарисованы настоящим
`left_click_drag`, выделены оба через Shift-клик по строкам панели слоёв,
сгруппированы кнопкой «Сгруппировать» — всё реальными кликами `computer`, не
вызовами API. Группа повёрнута на 30° (матрица выставлена напрямую —
готового жеста поворота фигуры в интерфейсе пока нет, это записано в
ограничениях ниже). Экранный `getBoundingClientRect()` прямоугольника:

до разгруппировки — `{x: 121.8425521850586, y: 358.8525390625, width:
114.6384506225586, height: 102.5595703125}`; после — те же четыре числа, ни
одна цифра не отличается. Это и есть «фигуры остаются там, где были»,
проверенное не рассуждением, а замером в работающем браузере.

**Ловушка метода, найденная и записанная в `CLAUDE.md`.** Клики `computer` по
координатам из `getBoundingClientRect()` стабильно промахивались мимо строк
панели. Причина — скриншот `computer` живёт в своей системе координат (800×750),
а `getBoundingClientRect()` отдаёт CSS-пиксели (у страницы — 846×794); нужен
масштаб `ширина_скриншота / window.innerWidth`, а не единица.

#### Известные ограничения

- **Нет жеста поворота фигуры/группы в интерфейсе.** Проверка выше вращала
  группу прямой записью в `transform` через консоль — сам механизм
  трансформации проверен, ручка на холсте, за которую можно потянуть, ещё не
  сделана. У прямоугольника/эллипса/картинки раньше был `rotation`, но и у
  него никогда не было ручки — это не регресс этого этапа, а нерешённая
  задача, которая теперь хотя бы имеет на чём быть решённой.
- **Group/Ungroup в меню «Объект» не привязаны к физической клавиатуре** —
  показывают `Ctrl+G`/`Ctrl+Shift+G` как текст, как и остальные пункты этого
  меню (Duplicate, Delete и т. д.) уже делали до этого этапа. Не новая дыра,
  а тот же пробел, что был всегда в этом меню; настоящая привязка требует
  регистрации команд в каталоге (`commands/definitions`), как у растровых
  действий, и осталась за скобками этого этапа.
- **Артборды заведены как модель данных**, но создавать/двигать их через
  интерфейс негде — только флажок «Включить монтажные области» при создании
  документа, который теперь сеет один артборд размером с холст.
- **Единицы измерения (`units.ts`) и структура `Color` в ядре сделаны и
  протестированы**, но большинство числовых полей свойств (X, Y, ширина,
  радиус угла) всё ещё принимают только пиксели — подключение `parseLength`
  к каждому такому полю не входило в объём этого этапа.

### Этап 3. Настоящая геометрия на TypeScript — сделано 5 сентября 2026

Цель: габариты и попадание, которым можно верить.

- [x] Габарит пути по кривым (`geometry.ts`'s `cubicBezierBounds`/
      `pathSegmentBounds`), а не по опорным точкам; ноль из `Math.min` убран
- [x] Габарит с учётом трансформаций родителей — уже было решено этапом 2
      (`shapeWorldBounds`), здесь только подтверждено, что локальные габариты
      под ним стали точными
- [x] Габарит с учётом ширины обводки — `padForStroke`, полширины на каждую
      сторону, когда у фигуры есть обводка
- [x] Попадание по заливке (ненулевое правило, `pointInPolygon`) и отдельно
      по обводке (`distanceToPolyline`) — единый алгоритм на все виды фигур
      через контур (`roundedRectOutline`/`ellipseOutline`/`flattenPathOutline`),
      а не отдельная формула на каждый вид
- [x] Метрики текста через `measureText` — `vector-text-metrics.ts` в
      apps/web, переиспользует `textFontString` из `textRender.ts`; сам пакет
      `env-vector` остаётся без DOM, измеритель — внедряемый порт
      (`TextMeasurer`), с прежней формулой-оценкой как откат без канваса
- [x] Каждый пункт — тест, доказанный падающим на нынешнем коде: 21 тест на
      `geometry.ts`, 12 на `hit-test.test.ts` (7 из них ловят откат к старой
      проверке по прямоугольнику габарита), канарейка бага §2.1 заменена на
      тест правильного результата

**Живая проверка настоящим кликом.** На dev-сервере: прямоугольник 300×300 со
скруглением 130 (почти круг) — клик в угол его габаритной рамки (внутри
bbox, снаружи видимой фигуры) не выделяет ничего; клик в центр выделяет.
Текст «Wi» 60px: измеренные границы — 69.96×61 (`actualBoundingBoxAscent`/
`Descent` из настоящего канваса), а не старая формула
`длина×кегль×0.55`/`кегль×1.3`, которая дала бы 66×78 — числа отличаются
именно там, где формула была лишь прикидкой.

**Не изменившееся архитектурное решение, стоит подчеркнуть.** «Габарит с
учётом трансформаций родителей» был уже закрыт этапом 2 — `shapeWorldBounds`
существовала до этого этапа и составляла матрицу предков независимо от
точности локальных габаритов под ней. Этот этап не трогал эту часть, только
сделал сами локальные габариты (то, что `shapeWorldBounds` трансформирует)
честными.

#### Известные ограничения

- **Допуск клика по линии не зависит от масштаба.** `MIN_LINE_HIT_TOLERANCE`
  — фиксированные 4 единицы в локальном пространстве фигуры; при разном
  зуме или масштабе в цепочке предков это разное число экранных пикселей.
  Настоящее исправление требует прокинуть эффективный масштаб от вызывающей
  стороны (`VectorWorkspace`) до `shapeAt`, что шире, чем эта фигурирующая
  здесь правка габаритов/попадания.
- **У текста по-прежнему только прямоугольная область попадания**, не контур
  букв — обводка текста как таковая не хит-тестится отдельно от заливки.
  Настоящие контуры глифов — задача этапа 11 (текст через Parley/HarfRust).
- **Попадание по обводке — точное расстояние до линии**, без учёта соединений
  (`stroke-linejoin`) и торцов (`stroke-linecap`) — угол толстой обводки
  хит-тестится чуть уже, чем видно на экране. Не считается существенной
  неточностью на этом этапе.

### Этап 4. Пространственный индекс — сделано 5 сентября 2026

Цель: поиск под курсором перестаёт зависеть от числа объектов.

- [x] RBush (`spatial-index.ts`) над мировыми габаритами фигур; обновление —
      `useMemo` в `VectorWorkspace.tsx`, keyed по `document.revision` (счётчик
      ядра, растущий на каждый `kernel.documents.update`) — индекс пакет
      `env-vector` сам не знает о ревизиях, это забота вызывающей стороны
- [x] `shapeAt` и рамка выделения ходят через индекс — `shapeAtIndexed` для
      клика, `shapeWorldBoundsIndexed` для рамки (кешированные габариты, а не
      второй проход по цепочке предков в каждом кадре)
- [x] Замер этапа 1 повторён на 10 000 фигурах
- [x] Тест: индекс и линейный перебор дают один ответ — 5 случайных
      документов по 300 фигур (со вложенными повёрнутыми группами), по 200
      случайных точек на документ, итого 1000 сравнений; ловит регрессию
      (проверено разворотом сортировки по порядку отрисовки — упало 5 из 5)

**Замер, и он честный в обе стороны.** Не только удобные числа:

| | было (линейный перебор) | стало (индекс) |
|---|---|---|
| Промах на 10 000 фигурах | ~3.4 мс | **~0.002 мс** |
| Попадание на 10 000 фигурах | ~1.4 мс | ~1.0 мс |
| Построение индекса на 10 000 фигурах | — | ~12.3 мс (разово на ревизию) |

Промах — самый частый случай в реальной работе (курсор чаще не наведён точно
на фигуру, чем наведён) — стал независим от числа фигур, что и было целью.
**Попадание же почти не ускорилось**, и причина в контрольном документе, а не
в индексе: 10 000 прямоугольников 160×100 в области 1800×1000 перекрываются
на сотни слоёв в глубину в любой точке, и после того как R-дерево нашло
кандидатов, разбор «кто из них сверху» всё равно требует настоящей проверки
попадания по каждому. Обычный документ так плотно не набит; число записано
как честный крайний случай, а не типичный, — чтобы следующий этап не
переоткрывал это заново.

**Живая проверка реальным вводом.** На dev-сервере: клик в угол габарита
скруглённой фигуры по-прежнему промахивается через новый индексированный
путь, клик в центр выделяет; перетаскивание реальным `left_click_drag`
двигает фигуру и рамка выделения (читаемая из кеша индекса) следует за ней
без рассинхронизации.

### Этап 5. Привязки — сделано 5 сентября 2026, частично

Цель: рисование становится точным.

- [x] `SnapEngine` (`snapping/engine.ts`) поверх индекса; **каждый вид
      привязки — файл в каталоге** (`snapping/sources/*.ts`) — реестр
      (`registry.ts`) собран вручную, не через `import.meta.glob`: у
      `env-vector` нет своей сборки Vite, чтобы такое автообнаружение
      вообще было возможно; добавление вида по-прежнему «файл плюс одна
      строка в реестре», а не ветка в switch
- [~] Виды: **сделаны** сетка, габариты (края и центры), узлы, середины
      сегментов, **и направляющие** (добавлено 6 сентября 2026, после того
      как stage 15 построил модель линеек/направляющих, которой раньше не
      хватало) — 5 источников, 31 тест. Новый источник —
      `snapping/sources/guides.ts`: `VectorGuide.orientation` уже называет
      ось, которую он ограничивает («vertical»-направляющая — вертикальная
      линия, ограничивает `x`), источник доверяет `context.guides` уже
      отфильтрованным вызывающей стороной через `visibleGuides` (тот же
      «получил нужный срез» контракт, что уже есть у `shapes`/`excludeIds`
      в этом каталоге), не передёргивает фильтрацию по `scope` второй раз.
      Подключено в оба места, что строят `SnapContext` вручную —
      `vector.select`'s драг и `vector.pen`'s привязка размещаемой точки.
      Живая проверка настоящим вводом: направляющая на x=300, фигура
      перетащена так, что левый край целился рядом (не точно на неё) —
      приземлилась ровно на 300. **Отложены**: пересечения, касательные,
      перпендикуляры, равные расстояния — настоящая геометрия пересечения
      кривых, отдельная большая задача
- [x] Радиус привязки в экранных пикселях — `VectorWorkspace.tsx` делит
      8 px на текущий зум перед вызовом движка; сам движок и источники
      ничего не знают про экран
- [x] Подсветка — `vector.select`'s Overlay, розовая пунктирная линия
      через весь холст на месте совпадения; цвет — фиксированный, как
      марширующие муравьи, добавлен в список исключений `tokens.test.ts`
- [x] Живая проверка настоящим вводом: `left_click_drag` подвинул
      прямоугольник (700,200) к другому (200,200,200×150) — приземлился
      **ровно** на x=400, y=200 (оба края целевой фигуры), не рядом.
      Кадр с самой розовой линией *посреди* перетаскивания не снят —
      инструмент браузера не даёт развести нажатие и отпускание одним
      действием; рендер подсветки использует тот же JSX-приём, что уже
      живьём проверен на `vector.nodes`' ручках

**Найдено попутно: `smartGuides` и `snapToGuides` были мёртвыми
чекбоксами** (правило 3 `CLAUDE.md`) — существовали в «Настройки → Направляющие
и сетка» с самого начала, но ничего в коде их не читало, ни в векторе, ни в
растре. `smartGuides` теперь честно включает/выключает источники этого
этапа. `snapToGuides` **остаётся мёртвым** — направляющих (линеек) у
вектора ещё нет, включать нечего; добавлена новая пара `snapToGrid`/
`snapGridSize`, оба читаются `VectorWorkspace.tsx` по-настоящему.

**Найдено попутно: клик выделения в векторе не использовал индекс этапа
4.** `select.ts`/`nodes.ts` вызывали линейный `shapeAt` напрямую — этап 4
подключил индекс только к правому клику контекстного меню. Исправлено:
`ToolContext` получил поле `spatialIndex`, `beginSelectDrag` теперь ходит
через `shapeAtIndexed`.

**У растра уже есть настоящие линейки и направляющие** (`RasterGuide` в
`@vravio/env-raster`, `raster-ruler-guides.tsx`) — можно перетащить
направляющую с линейки, она хранится в документе. Но ничего не *снаппится*
к ним: растровый инструмент перемещения не читает ни `smartGuides`, ни
`snapToGuides`, ни сами линии сетки. Это отдельная, реальная задача
(привязка в растре — по явному запросу владельца), не сделанная в этом
проходе; вектору для направляющих либо нужен свой `guides: RasterGuide[]`-
подобный список плюс перенос UI линеек, либо (лучше) вынести и линии, и
хит-тест в общий модуль ядра, чтобы у растра и вектора не было двух
инкосистентных реализаций одного и того же.

### Этап 6. Оформление: градиенты, стопки заливок и обводок — сделано 6 сентября 2026, частично

Цель: объект выглядит как в Illustrator, а не как одна заливка и одна линия.

- [x] Линейный и радиальный градиент, редактор остановок — `appearance.ts`'s
      `Gradient` (`from`/`to` нормализованы 0..1, как SVG
      `objectBoundingBox`, — в SVG-рендере это ноль лишней математики, в
      canvas-экспорте координаты масштабируются габаритами фигуры);
      `GradientEditor` в `AppearancePanel.tsx` — смена типа, остановки
      (мин. 2), добавление/удаление
- [x] Несколько заливок и обводок стопкой, порядок меняется — `FillLayer[]`/
      `StrokeLayer[]` в `VectorStyle`, каждый слой со своими paint/opacity/
      visible/blendMode; кнопки ↑/↓ в `AppearancePanel.tsx`
- [x] Пунктир, концы линий, соединения — `dash`/`cap`/`join` на
      `StrokeLayer`, доходят до SVG (`strokeDasharray`/`strokeLinecap`/
      `strokeLinejoin`) и до canvas-экспорта (`setLineDash`/`lineCap`/
      `lineJoin`) одинаково
- [x] Выравнивание обводки — `StrokeAlignment` (`center` | `inner` | `outer`)
      теперь редактируется (`AppearancePanel.tsx`'s `StrokeRow`, новый
      `<select>` сразу после соединений) и читается всеми тремя путями
      рендера: живой SVG-холст (`VectorWorkspace.tsx`'s `renderShape`), файл
      SVG-экспорта (`vector-svg-export.ts`'s новый `strokeMarkup`) и
      canvas-экспорт (`vector-environment.ts`'s `paintShape`). У SVG/canvas
      `stroke` нет нативного понятия «внутри/снаружи» — он всегда
      центрирован, поэтому во всех трёх местах техника одна: обводка рисуется
      двойной ширины, затем обрезается — `<clipPath>` для «внутри» (простое
      совпадение с собственной геометрией фигуры) и маска/клип для «снаружи»
      (нужно исключить именно внутренность фигуры, а не оставить только её).

      Первая попытка «снаружи» была `<clipPath clipRule="evenodd">`,
      содержащий два **отдельных** дочерних элемента (огромный прямоугольник
      + геометрия фигуры), в расчёте на то, что even-odd их скомбинирует так
      же, как скомбинировал бы два подпути одного `<path d="...">`. Новый
      пиксельный тест на реальном рендере (`vector-svg.crosscheck.test.ts`,
      через `resvg`, а не проверка строк разметки) поймал, что это неверно:
      пиксель на 3px внутрь от края фигуры, который «снаружи»-выравнивание
      обязано было исключить, получал полную непрозрачность — обводка шла
      прямо через незакрашенную внутренность фигуры, потому что even-odd не
      объединяет два раздельных дочерних элемента `<clipPath>` так, как
      объединяет подпути одного `<path>`. Починено переходом на `<mask>`
      (увеличенный белый прямоугольник плюс чёрная копия геометрии фигуры,
      нарисованная поверх) — цветовая композиция маски по яркости через
      раздельные элементы определена надёжно, что подтвердил тот же
      пиксельный тест после исправления (все 4 пиксельных проверки в файле
      зелёные, `edgeBlue > 150` там, где должно быть закрашено,
      `alpha === 0` там, где не должно). На canvas-пути аналог того же
      приёма — `context.clip(path, "evenodd")` на **одном** `Path2D`,
      содержащем оба подпути (`outerClip.rect(...)` + `outerClip.addPath
      (path)`) — это уже тот случай, который у resvg работал (один путь, а
      не два раздельных элемента), так что для canvas even-odd-клип остаётся
      корректным приёмом без перехода на маску.

      Выравнивание осмысленно только для фигур с внутренностью
      (прямоугольник/эллипс/путь, `pathFor`/`geometryElement` возвращают
      что-то с площадью) — для линии и текста, где перекрытие
      внутри/снаружи not defined, все три пути рендера рисуют по центру
      независимо от настройки, тот же вырожденный случай во всех трёх
      местах, не расхождение между ними.

      Проверено: живая проверка в браузере (реальное DOM-событие `change` на
      настоящем `<select>` — не прямая мутация модели — переключала
      alignment с "center" на "inner", затем на "outer", каждый раз
      подтверждено чтением фактической разметки живого SVG:
      `<clipPath id="stroke-align-...">` для inner,
      `<mask id="stroke-align-...">` с прямоугольником 200000×200000 для
      outer), плюс 3 новых теста строк разметки в
      `vector-svg-export.test.ts`, 4 новых пиксельных теста через resvg в
      `vector-svg.crosscheck.test.ts`, и 4 новых теста canvas-пути в
      `vector-environment.test.ts` (dead-checkbox: center/inner/outer дают
      разные вызовы `lineWidth`/`clip`, линия игнорирует настройку). Полный
      прогон — 1088 тестов, 76 файлов, все зелёные; `tsc --noEmit` чисто на
      всех 4 пакетах.
- [x] Режимы наложения на уровне объекта — `VectorStyle.blendMode`,
      переиспользован `RasterBlendMode` из `@vravio/env-raster` (26 режимов
      Photoshop), а не заведён свой дубликат-энум; в SVG — CSS
      `mix-blend-mode` на обёртывающем `<g>`, в canvas-экспорте —
      `globalCompositeOperation`. Режим наложения **отдельного слоя**
      заливки/обводки в модели есть и работает в SVG (`mix-blend-mode` на
      каждом `<Tag>`), но **не поддержан в canvas-экспорте** — корректная
      изоляция композитинга внутри одной фигуры требует offscreen-канвас на
      фигуру, которого у этого пути нет; задокументировано прямо в
      `vector-environment.ts`'s `paintShape`
- [x] Правило мёртвого флажка: каждая объявленная опция обязана менять
      картинку, и на это есть тест — `vector-environment.test.ts`, 12
      тестов на `paintShape` с поддельным `CanvasRenderingContext2D`,
      записывающим вызовы (jsdom не даёт настоящий canvas); проверено
      разворотом `setLineDash` в `[]}` — тест на пунктир упал, как и
      должен

**Косметический долг, оставленный открытым.** `BlendMode` — это
переэкспортированный `RasterBlendMode`, и имя всё ещё говорит "Raster…",
хотя тип теперь общий для обеих сред. Переименование задело бы растровый
пакет ради вектора — вне рамок этого этапа.

**Живая проверка реальным вводом.** На dev-сервере: прямоугольник, заливка
color → gradient — на холсте появился настоящий линейный градиент
чёрный→белый (проверено через DOM: `<linearGradient
gradientUnits="objectBoundingBox">` в `<defs>`, `fill="url(#…)"` на
`<rect>`); добавлена обводка, ширина 2→8, пунктир solid→dashed, cap
butt→round, join miter→bevel — все пять значений подтверждены через
`getAttribute` на живом `<rect>` (`stroke-width="8"`,
`stroke-dasharray="16 16"`, `stroke-linecap="round"`,
`stroke-linejoin="bevel"`); режим наложения объекта normal→multiply дал
`style="mix-blend-mode: multiply"` на обёртывающем `<g>`; непрозрачность
объекта 100→50 дала `opacity="0.5"` на том же `<g>`.

### Этап 7. Порт геометрии и первая операция в WASM — сделано 6 сентября 2026

Цель: появилась вторая реализация тяжёлой математики, и доказано, что она даёт
то же самое.

- [x] `VectorGeometryPort` в ядре (`kernel/src/vector-geometry-port.ts`):
      плоский `[x0,y0,x1,y1,...]`-массив на входе, массив таких же плоских
      массивов на выходе — ни одна реализация не знает о представлении
      фигур `env-vector`, только об этих числах
- [x] Реализация на TS — эталонная, медленная
      (`kernel/src/vector-geometry-reference.ts`), поверх `polygon-clipping`
      (проверенная реализация Мартинеса-Руэды), а не написанная с нуля —
      тот же выбор, что уже был сделан для `rbush` на этапе 4: самодельный
      полигональный клиппер — ровно то, что выглядит готовым задолго до
      того, как станет действительно корректным
- [x] Крейт `crates/vector-geometry`, сборка в WASM, ленивая загрузка —
      `apps/web/src/vector-geometry-wasm.ts` грузит `.wasm` только по
      первому вызову `booleanOp`, не при старте приложения; `pkg/` (вывод
      `wasm-pack`) закоммичен в репозиторий, а не собирается на каждой
      машине — иначе `apps/web` не завёлся бы у любого, кто клонировал
      репозиторий без установленного Rust
- [~] Булевы операции — **не через Clipper2**. Крейт `clipper2` из
      crates.io оборачивает C++-библиотеку Clipper2 (зависит от
      `clipper2c-sys`/`libc`) и не собирается под
      `wasm32-unknown-unknown` без Emscripten — тяжёлого тулчейна поверх
      уже поставленного Rust+wasm-pack. Вместо этого — `geo-booleanop`
      (крейт, чистый Rust, тот же алгоритм Мартинеса-Руэды, что и у
      TS-эталона, только на другом языке): объединение, вычитание,
      пересечение, исключение — все четыре реализованы и работают.
      **Дыры есть, и они честные**: результат без учёта отверстий (фигура
      с дыркой после вычитания приходит как отдельный полигон снаружи, а
      не как кольцо внутри — одно и то же упрощение с обеих сторон,
      задокументировано в `vector-geometry-port.ts`), только простые
      многоугольники на входе (кривые контуров ещё не превращаются в
      полилинии перед клиппингом — этим предстоит заняться, когда булевы
      операции реально понадобятся инструменту, не раньше)
- [x] **Один набор тестов прогоняется на обеих реализациях**
      (`apps/web/src/vector-geometry.parity.test.ts`, 21 тест: 5 фигур ×
      4 операции плюс тест на невырожденность) — сравнение не по массиву
      вершин (два разных алгоритма не обязаны отдавать точки в одном
      порядке или направлении обхода даже для одинаковой формы), а по
      плотной сетке точек «внутри/снаружи» над общим прямоугольником
      фигур, со смещением на нецелые координаты, чтобы точка никогда не
      легла ровно на общее ребро. Проверено разворотом `union` на
      `intersection` внутри крейта — упали ровно 4 теста из 21 (все
      про `union`, кроме случая с одинаковыми квадратами, где объединение
      и пересечение — одна и та же фигура, как и должно быть)
- [x] Замер: **~3.4–4.7× быстрее** на объединении двух 64-угольников
      (несколько прогонов `vector-geometry.bench.test.ts`, самый быстрый
      результат за 20 повторов): TS-эталон ~0.65–0.93 мс, WASM ~0.18–0.20 мс.
      Число честное для этого размера входа — на момент замера ни один
      настоящий вызов булевой операции из UI ещё не существовал, чтобы
      измерить более реалистичную нагрузку; тест это прямо оговаривает в
      своих комментариях, а не выдаёт локальный замер за нагрузочный.
      **Обновление 6 сентября 2026**: теперь существует — см. Pathfinder
      ниже
- [x] **UI-инструмент, вызывающий движок — Pathfinder** (Object → Обработка
      контуров: Объединить/Вычесть/Пересечь/Исключить). Раньше самый
      сильный пункт этого этапа был честным, но абстрактным: движок
      булевых операций был построен, перепроверен и измерен, но ни одна
      команда меню его не вызывала. Закрыто 6 сентября 2026, в той же
      сессии, что построила линейки/направляющие: два новых чистых модуля
      в `packages/env-vector/src/boolean-ops.ts` —
      `shapeOutlineWorldPolygon` (превращает прямоугольник/эллипс/путь в
      плоский полигон в мировых координатах, переиспользуя тот же контур,
      что уже строит `hitTestShape` для тех же трёх видов фигур, а не
      вторую реализацию, которая могла бы разойтись с тем, что реально
      видно на экране) и `pathShapeFromPolygon` (обратное направление —
      полигон обратно в реальную фигуру-путь). `apps/web/src/vector-
      commands.ts`'s `applyPathfinderOp`: полностью асинхронный расчёт
      (WASM `booleanOp` синхронного пути не имеет) выполняется целиком
      **до** вызова `changeVectorDocument`, так что его собственный
      `mutate`-колбэк остаётся простой синхронной функцией, применяющей
      уже готовый результат — тот же контракт, которого `changeVectorDocument`
      и так требует от любого вызывающего. Фигуры выделения объединяются
      попарно в порядке `state.shapes` (как реально выглядит стопка на
      холсте, а не порядок кликов, добавивших их в `state.selection`);
      если промежуточный шаг (вычитание/исключение) даёт больше одного
      полигона (собственное упрощение движка «дырка = отдельный внешний
      полигон», см. `FlatPolygon`'s doc comment), каждый следующий шаг
      цепочки прогоняется по **каждому** уцелевшему куску, и каждый
      уцелевший кусок в конце становится своей фигурой-результатом, а не
      молча теряется.

      Честно унаследованные ограничения самого движка, не сужены тихо:
      без дырок (кольцо внутри), результат — многоугольник с прямыми
      рёбрами, не аппроксимированный обратно в кривые (см. следующий пункт
      этого же файла про `Обратная аппроксимация`) — прямолинейный
      результат Pathfinder сегодня, честно так подписанный, лучше, чем
      никакого Pathfinder вообще.

      Тесты: 7 новых в `boolean-ops.test.ts` (превращение
      прямоугольника/эллипса/пути в мировой полигон с учётом трансформации,
      `null` для фигур без единого контура заливки — группа/линия/текст,
      обратное построение фигуры из полигона). Живая проверка настоящим
      кликом по меню: два перекрывающихся прямоугольника → «Объединить»
      дал одну L-образную 8-точечную фигуру-путь, отменено (`Ctrl+Z`)
      вернуло оба прямоугольника на место, «Вычесть» дал 6-точечную
      L-образную фигуру (прямоугольник A без перекрывающегося угла с B) —
      оба результата подтверждены и численно (число точек, имя фигуры), и
      визуально (скриншот).

**Установленный на этой машине тулчейн**: `rustup` (через winget),
`wasm32-unknown-unknown` target, `wasm-pack` — по явному запросу владельца
после вопроса «ставить ли тяжёлый тулчейн ради этого этапа».

### Этап 8. Кривые всерьёз: офсет, обводка в контур, упрощение — сделано 6 сентября 2026, частично

Цель: операции, после которых остаются кривые, а не тысяча точек.

- [x] Офсет пути (Kurbo) — `offset_path` в `crates/vector-geometry`
      (тот же крейт, что и булевы операции этапа 7, не новый — один `.wasm`,
      одна ленивая загрузка на оба этапа), поверх `kurbo::expand_path`;
      читает и пишет тот же формат SVG `d`-строки, что уже производит и
      потребляет `pathData()` в `env-vector` — никакой новой схемы кодирования
      не понадобилось, в отличие от плоских массивов этапа 7
- [x] Преобразование обводки в контур — `stroke_to_fill`, поверх
      `kurbo::stroke`; ширина/колпачки/соединения/пунктир запекаются в
      геометрию один раз, дальше это обычная заливка
- [x] Упрощение пути с заданной точностью — `simplify_path`, поверх
      `kurbo::simplify_bezpath`. Работает по-настоящему: почти прямая линия
      из 50 точек с малым шумом схлопывается до 2 команд при
      разумной точности (замерено)
- [~] Обратная аппроксимация полигонов Clipper2 в кривые — **не получилось**,
      и это не тихо забытый пункт. `simplify_bezpath` по умолчанию (и с
      подобранным вручную более широким порогом угла) сохраняет любой
      неслучайный поворот как настоящий угол — развёрнутая в полигон
      окружность неотличима от 64-угольника, который кто-то нарисовал
      специально: приблизительно 5° поворота на каждой вершине уже выше
      порога «это шум». **Замерено**: правильный 64-угольник-окружность
      остаётся на 65 командах пути при точности от 0.01 до 10 (10% от
      собственного радиуса) — упрощение не срабатывает вообще; с вручную
      расширенным порогом угла (0.2 рад) и точностью 20 — падает только до
      27 команд, не до единиц. Настоящее решение — аппроксимация методом
      наименьших квадратов через `kurbo::fit_to_bezpath` с собственной
      реализацией `ParamCurveFit` над последовательностью точек, а не
      сохраняющее-углы упрощение; это отдельная, реальная задача, отложенная
      до момента, когда булева операция понадобится инструменту в UI
- [x] Замер числа узлов после объединения двух окружностей: **цель не
      достигнута, число записано честно**. Union двух 64-угольников (радиус
      100, наложение наполовину) даёт полигон на 84 вершины; после
      `simplify_path` с точностью 1 (настройки по умолчанию, без ручного
      расширения порога угла) — **85 команд пути**, практически без
      сокращения, не «единицы». Число закреплено тестом
      (`vector-geometry.curves.test.ts`, «measured: unioning two 64-sided
      circles does NOT come back down to single-digit nodes yet») как
      честный текущий пол, а не как достижение — чтобы будущая попытка
      настоящей аппроксимации кривыми имела с чем сравниться, и чтобы этот
      пробел не мог незаметно оказаться «вроде решённым»

**Тесты**: `vector-geometry.curves.test.ts`, 8 тестов на реальном
скомпилированном `.wasm` — офсет наружу/внутрь (офсет внутрь проверен на
32-угольнике-окружности, не на квадрате: у `expand_path` с отрицательным
расширением на прямоугольных углах, по собственному предупреждению Kurbo,
получаются самопересечения — замерено на квадрате и подтверждено), что
`round`-соединение действительно добавляет кривые в офсете, а `miter` —
нет; обводка в контур даёт заливку ожидаемого размера, `round`-колпачок
продлевает её за концы линии, `butt` — нет; упрощение схлопывает шум и
не является вырожденным (проверено на разнице между точным и грубым
допуском).

### Этап 9. Недеструктивные модификаторы — сделано 6 сентября 2026, частично

Цель: то, ради чего вся эта архитектура.

- [x] Стопка `geometry[]` на сущности, **модификатор = файл в каталоге** —
      `env-vector/src/modifiers/`, один файл на вид в `kinds/`, вручную
      перечисленные в `registry.ts` — тот же каталожный приём, что уже
      применён к привязкам (`snapping/registry.ts`) и по той же причине:
      у `env-vector` нет своей сборки Vite для `import.meta.glob`. Схема
      бампнута v4 → v5, миграция добавляет пустой `geometry: []` каждой
      существующей фигуре — чисто аддитивно, пустая стопка ведёт себя как
      её отсутствие
- [x] Первые: скругление углов, офсет, упрощение, зигзаг, булева с другой
      фигурой — все пять сделаны. `roundCorners`/`zigzag` — честный чистый
      TS без WASM; `offset`/`simplify` идут через `VectorCurvePort` этапа 8;
      `boolean` — через `VectorGeometryPort` этапа 7, читает **собственный**
      базовый контур другой фигуры (`resolveShapePath`), а не то, во что её
      превратил её же стек модификаторов — иначе две фигуры, булево
      применённые друг к другу, могли бы зациклить пересчёт одна через
      другую
- [x] Кеш результата по ревизии; инвалидация при правке источника —
      `apps/web/src/vector-modifiers.ts`'s `useModifierResults`, тот же
      грубый сигнал инвалидации (`document.revision`), что уже использует
      пространственный индекс этапа 4; пока WASM-модификаторы фигуры ещё
      не пересчитались, она рисуется собственной немодифицированной
      геометрией — никогда не пусто и не устаревшим чужим результатом
- [x] Панель свойств показывает стопку — **порядок меняется кнопками
      вверх/вниз, не перетаскиванием**. То же самое честное упрощение, что
      уже сделано в `AppearancePanel.tsx` для стопок заливок/обводок этапа
      6 — а не недосмотр
- [x] Проверка, ради которой всё затевалось — **пройдена вживую в
      браузере**: прямоугольник 160×100, стопка [скругление радиусом 8,
      офсет +10] → путь шириной ~180 (160 + 2×10). Ширина прямоугольника
      изменена на 400 через панель свойств — путь пересчитался
      автоматически, стал шириной ~420 (400 + 2×10), без перезагрузки и
      без ручного триггера

**Тесты**: `env-vector/src/modifiers/apply.test.ts` (10 тестов — чистые
TS-модификаторы и оркестратор `applyModifierStack`, включая проверку
невырожденности: изменение ширины прямоугольника между двумя вызовами даёт
разный результат) и `apps/web/src/vector-modifiers.test.ts` (5 тестов
против настоящего скомпилированного `.wasm`: офсет, упрощение, булева
операция с другой фигурой — включая случай, когда фигура-операнд удалена, —
и собственно проверка этапа: скругление → офсет → смена ширины →
пересчёт). React-хук `useModifierResults` отдельным юнит-тестом не
покрыт — в проекте нет `@testing-library/react`, его кеш-по-ревизии
поведение проверено вживую в браузере вместо этого.

### Этап 10. SVG: импорт и экспорт — сделано 6 сентября 2026, частично

Цель: документами можно обмениваться с остальным миром.

- [~] Импорт через `usvg` (WASM): новый крейт `crates/vector-svg` (отдельно
      от `vector-geometry` этапа 7/8 — иначе каждый вызов `boolean_op`
      тянул бы за собой шейпинг шрифтов, никому не нужный для булевой
      операции). **Пути** и **градиенты** — сделаны и покрыты тестами на
      реальном `.wasm` (`vector-svg-import.test.ts`). **Трансформации** —
      сделаны, но через честное упрощение: **группы разворачиваются в
      плоский список**, каждая фигура получает свою корректную абсолютную
      трансформацию (сумму всех трансформаций предков), но исходная
      иерархия `<g>` не восстанавливается — выделить/подвинуть бывшую
      группу как одно целое после импорта не получится. **Текст —
      пропускается совсем**, не превращается в контуры и не импортируется
      как редактируемый: настоящий шейпинг требует загруженных шрифтов в
      `fontdb`, а это инфраструктура этапа 11, а не этого. Полу-шейпинг без
      неё был бы хуже честного "пока не умеем". Паттерны (`<pattern>`)
      заменяются плоским серым — тоже честно, не молча
- [x] Экспорт в SVG из семантической модели — `apps/web/src/vector-svg-export.ts`,
      чистый TS без WASM (в эту сторону `usvg` не нужен: наша модель уже
      однозначно определяет разметку). Сознательно близко к
      `VectorWorkspace.tsx`'s `renderShape` (тот же порядок стопок
      заливок/обводок, та же обработка градиентов), чтобы то, что видно в
      редакторе, и то, что пишется в файл, не могли разойтись незаметно
- [x] Сверка с `resvg` как эталоном — **не тем способом, что предполагался**.
      В проекте нет `window`/`HTMLCanvasElement` под Node-окружением
      Vitest (ни jsdom, ни `node-canvas`), поэтому нельзя было отрендерить
      наш собственный canvas-экспорт (этап 7's `vector-environment.ts`) в
      том же тестовом процессе, чтобы сравнить его с resvg попиксельно.
      Вместо этого — resvg проверен против **аналитически посчитанных**
      значений пикселей (цвет заливки ровно там, где фигура; альфа=0 там,
      где фигуры нет) — `vector-svg.crosscheck.test.ts`, 3 теста,
      непустой при развороте цвета. Буквальное «resvg против живого
      редактора» прогнано один раз вручную в браузере (там есть настоящий
      Canvas) — см. запись ниже
- [x] Набор регрессионных файлов — 16 настоящих чужих SVG из
      simple-icons (CC0, github.com/simple-icons/simple-icons), не
      нарисованных под себя (`vector-svg.regression.test.ts`, 17 тестов:
      счётчик ≥12 плюс по одному на файл — каждый импортируется без
      исключения и даёт хотя бы одну фигуру). **Честная дыра в самом
      наборе**: simple-icons — это всегда один плоский путь без градиентов
      и групп; настоящий файл с градиентом/группами под понятной открытой
      лицензией не найден за отведённое на этот этап время. Импорт
      градиентов и трансформаций групп проверен только на собранных вручную
      фикстурах (`vector-svg-import.test.ts`), не на настоящем чужом файле
      — более узкая гарантия, чем «настоящий файл с градиентом парсится
      правильно»

**Живая проверка в браузере**, полный цикл на реальном dev-сервере:
SVG-файл с синим прямоугольником (10,10)-(60,60) и розовым кругом
(центр 70,70, радиус 15) подан в скрытый `<input type=file>` через
`DataTransfer` (реального выбора файла в системном диалоге инструмент
браузера не даёт) — открылся новый векторный документ 100×100 с обеими
фигурами, правильными цветами и позициями (снято скриншотом). Тот же
документ экспортирован через «Файл → Экспортировать в SVG…»
(`window.showSaveFilePicker` на время проверки подменён, чтобы перехватить
`Blob` до его ухода в файл): результат —
`<path d="M10 10 L60 10 L60 60 L10 60 L10 10 Z" fill="rgba(51, 102, 204, 1)">`
для прямоугольника и корректная кубическая аппроксимация окружности с
`fill="rgba(204, 51, 102, 1)"` для круга — оба цвета и обе геометрии
совпадают с исходным файлом. Импорт → рендер → экспорт — весь цикл
подтверждён вживую, не только в тестах.

**Тесты**: 26 всего — `vector-svg-import.test.ts` (6), `vector-svg-export.test.ts`
(6), `vector-svg.crosscheck.test.ts` (3), `vector-svg.regression.test.ts` (17,
включая счётчик).

**UI-обвязка сделана**, не только библиотечный код: пункт меню «Файл →
Импортировать SVG как вектор…» (открывает новый векторный документ,
размер — из `width`/`height`/`viewBox` самого файла) и «Файл →
Экспортировать в SVG…» (сохраняет через тот же `kernel.platform.fs.saveFile`,
что и остальные экспорты в приложении) — оба в `App.tsx`, оба проверены
вживую выше. Сознательно **не** трогалось существующее поведение: `.svg`,
выбранный через обычный «Импортировать…», по-прежнему растеризуется в
растровый документ, как и раньше — новый пункт меню это отдельное,
осознанное действие «открыть как вектор», а не тихая замена уже
работающего пути.

### Этап 11. Текст — сделано 6 сентября 2026, частично

Цель: текст перестаёт быть строкой с выдуманной шириной.

- [x] Раскладка через Parley/HarfRust: лигатуры, кернинг, bidi, переносы —
      новый крейт `crates/vector-text` (третий WASM-крейт наравне с
      `vector-geometry` этапов 7/8 и `vector-svg` этапа 10, отдельный
      потому что несёт ~3.4 МБ шрифтов, которые никому больше не нужны).
      `layout_text(text, fontSize, maxWidth, script)` — настоящая раскладка:
      перенос строк по `maxWidth`, bidi-переупорядочивание Parley,
      шейпинг через HarfRust с реальным кернингом. **Не заменяет**
      `vector-text-metrics.ts`'s существующий измеритель на
      `canvas.measureText` — тот уже даёт честную ширину для однострочного
      случая через собственный текстовый стек браузера; этот крейт —
      портируемый движок раскладки, не зависящий от браузера, работающий
      одинаково офлайн и под Vitest, и умеющий то, чего у
      `CanvasRenderingContext2D` вообще нет — переносы строк и позиции
      отдельных глифов
- [~] Текст в рамке и текст по контуру — **не сделано**: `Layout` из
      Parley уже считает достаточно (перенос по ширине, позиции глифов
      вдоль строки) для обоих случаев, но подключение к живому
      редактированию в `VectorWorkspace.tsx` — отдельная, более крупная
      работа по UI, не сделанная в этом проходе
- [~] Курсор, выделение, попадание по символу — **не сделано**: та же
      причина, те же данные уже доступны (`GlyphResult.x/y/advance` на
      каждый глиф), просто не подключены к интерактивному редактированию
- [~] Преобразование текста в кривые — **не сделано**: нужен API контуров
      `skrifa` поверх уже шейпленных глифов (`skrifa::outline`,
      использован в собственном примере Parley) — реализация, а не
      исследование, но за это проход не взялись
- [x] Проверка на арабском и деванагари — три шрифта Noto Sans (OFL,
      github.com/google/fonts) встроены в `.wasm` через `include_bytes!`:
      латиница/кириллица, арабский, деванагари. `vector-text.test.ts`,
      8 тестов на реальном скомпилированном `.wasm`: измеренная ширина
      реагирует на кернинг (`"AVAVAVAVAV"` ≠ `"AAAAAAAAAA"` по ширине при
      одинаковом числе символов и размере шрифта), перенос строк
      действительно укладывается в `maxWidth` и даёт больше строк при
      более узком ограничении, арабский и деванагари шейпятся в
      несколько глифов с различными позициями (не проваливаются в пустой
      / однострочный fallback), диапазоны строк по байтам покрывают
      исходный текст без дыр и пересечений. Невырожденность подтверждена
      разворотом `break_all_lines` на фиксированную ширину — упали ровно
      3 теста, зависящих от переноса, остальные 5 остались зелёными

### Этап 12. GPU-рендер, если замеры этого потребуют

Цель: решение, принятое по цифрам.

- [x] Замер текущего SVG/DOM-рендера живьём, а не в vitest — сделано
      6 сентября 2026
- [ ] Тесселяция через Lyon
- [ ] Бэкенд поверх WebGPU (Vello либо свой)
- [ ] Сглаживание — у Lyon его нет, это отдельная работа
- [x] Сравнительный замер с SVG- и Canvas-бэкендами на одном документе —
      сделан только для текущего SVG-бэкенда (см. ниже); Canvas- и
      GPU-бэкендов ещё не существует, сравнивать не с чем
- [ ] **Право не делать этот этап**, если Canvas закрывает потребность —
      решение по цифрам ниже: **не закрывает**, при достаточно большом
      документе

**Как мерили.** Не `vitest` — jsdom не красит пиксели, число тут может
соврать. Мерили в реальном браузере (Chrome через `preview_start`):
живой `VectorWorkspace`, документ подменён напрямую через
`kernel.documents.update` на N прямоугольников, разбросанных по холсту
4000×4000, вид — «по размеру окна» (все N фигур одновременно в кадре, не
за его пределами). Затем — 2 секунды `requestAnimationFrame`-цикла,
меняющего `transform: scale(...)` на `.vector-stage` (тот же вид нагрузки,
что даёт живой zoom-жест), с логом `performance.now()`-дельт между
кадрами.

**Цифры** (это машина и день автора, не универсальная константа — см.
раздел 2 CLAUDE.md про такие пороги):

| Фигур | Средний кадр | Худший кадр |
|---|---|---|
| 50 | 16.7 мс (60 fps) | — |
| 500 | 16.6 мс (60 fps) | 17.1 мс |
| 1 500 | 16.6 мс (60 fps) | 16.8 мс |
| 2 500 | **1 300 мс** | **3 065 мс** |
| 3 000 | **2 169 мс** | **5 983 мс** |

Обрыв — не постепенная деградация, а стена между 1 500 и 2 500: почти
идеальные 60 fps резко сменяются секундами на кадр. Это не свойство
конкретно scale-жеста и не артефакт первого кадра — весь диапазон 2–3
тыс. одинаково плох.

**Что это НЕ значит.** У `renderShapeTree` (VectorWorkspace.tsx) сейчас
нет отсечения невидимых фигур — рендерятся все фигуры документа, а не
только видимые в текущем кадре. R-tree для этого уже есть
(`shapesInRect`, spatial-index.ts — заведён этапом 4, но ещё не
использован ни одним вызывающим кодом). Отсечение по вьюпорту решило бы
случай «документ большой, но пользователь смотрит на малую его часть» —
частый в реальной работе. Но именно этот замер поставил вид «по размеру
окна», то есть все фигуры и так были в кадре одновременно — отсечение
невидимых здесь ничего не убрало бы. Значит, стена — это стоимость
именно одновременно видимых узлов DOM/SVG при пересчёте стиля/layout на
масштабировании, а не следствие того, что можно почти бесплатно вырезать
отсечением.

**Решение по цифрам.** Текущий SVG-бэкенд не закрывает потребность при
документах в тысячи одновременно видимых фигур (плотная иллюстрация,
импортированный сложный SVG, набор иконок на одном холсте) — это
реалистичный, не надуманный размер. Право пропустить этап (последний
пункт чеклиста) не реализуется.

Но это не значит «делать Lyon+WebGPU прямо сейчас». Это отдельный,
недешёвый кусок работы — новый Rust-крейт с тесселяцией, свой
WebGPU-пайплайн, отдельная работа над сглаживанием (у Lyon его нет),
сравнительный харнесс с реальным Canvas-бэкендом (которого тоже пока
нет). Начинать его вслепую в рамках текущего прохода — противоречит
собственному правилу проекта не оставлять недоделанные куски. Прежде чем
браться за тесселяцию, стоит отдельно оценить более дешёвую промежуточную
меру — отсечение по вьюпорту через уже готовый `shapesInRect` — которая
решает частый случай (большой документ, маленький видимый кусок), но
честно не решает измеренный здесь случай (всё видно сразу). Обе меры
не взаимоисключающие: отсечение стоит сделать в любом случае, GPU-рендер
остаётся отдельным этапом для случая «много видно одновременно».

---

## 6. Чего этот план осознанно не делает

- **Не переносит Graphite целиком.** Его узловой граф — другая модель документа.
  Берём идеи и алгоритмы.
- **Не делает Paper.js фундаментом.** Его `Project`/`Layer`/`Item` конкурировали
  бы с ядром VRAVIO.
- **Не начинает с Rust.** Причина в 3.2.
- **Не обещает уровень Illustrator в булевых операциях с первого захода.**
  Ограничение Clipper2 названо явно.
- **Не трогает растровую среду.** Общими остаются ядро, ассеты и история.

---

## 7. Ответы владельца и что из них следует

Получены 5 сентября 2026 года. Каждый ответ меняет модель, поэтому записан не
как «выбрано то-то», а вместе со следствием.

### 7.1. Артборды — по-иллюстраторски

Монтажные области **размечают холст, но не владеют объектами**. Объект может
лежать между артбордами или наполовину снаружи, и это нормальное состояние, а
не ошибка.

Следствие для модели: артборд — не узел дерева, а запись в отдельном списке
документа. Сейчас в `VectorDocumentState` стоит `artboards: boolean` — флаг
«показывать ли их вообще». Он становится `artboards: Artboard[]` с именем,
прямоугольником и настройками экспорта у каждого.

Это заметно проще фигмовских фреймов: не нужно ни обрезание по контейнеру, ни
принадлежность, ни авторазметка. Дерево документа остаётся деревом объектов.

### 7.2. Единицы — пиксели и всё остальное

Нужны px, мм, см, дюймы и пункты.

Следствие: у документа появляется разрешение (ppi) и текущая единица
отображения. **Хранение остаётся в пикселях**, перевод — в одном модуле с
тестами на каждую пару единиц.

Почему хранение в пикселях, а не в пунктах, как у Illustrator: в VRAVIO вектор
живёт бок о бок с растром и обменивается с ним ассетами, у растрового документа
разрешение уже есть, и общая единица избавляет круговой обмен от пересчёта на
каждой границе. Плата за это честная и её надо назвать: **смена ppi документа
меняет физический размер всего**, поэтому она должна быть явной операцией с
выбором — пересчитать координаты или переистолковать их, ровно как «Размер
изображения» в растре.

Поля ввода должны принимать `10мм` и `0.5in` и переводить сами.

### 7.3. CMYK нужен

Это самый дорогой из четырёх ответов, и он меняет вещь, которую позже менять
больно.

Сейчас цвет — это `fill: string | null`, то есть строка `"#rrggbb"`. Строка не
умеет быть ни CMYK, ни плашечным цветом, ни цветом с профилем. Значит цвет
становится структурой:

```
Color {
  space:      "srgb" | "cmyk" | "gray" | "lab" | "spot"
  components: number[]
  profile?:   AssetId          // ICC-профиль лежит в хранилище ассетов
  alpha:      number
}
```

И живёт эта структура **в ядре, а не в векторном пакете**, потому что растру
CMYK нужен тоже — в `docs/requirements.md` это записано отдельной строкой
(`RASTER-COLOR-002`). Один тип цвета на обе среды — это та же «единственная
дверь», что и везде: иначе появятся два несовместимых цвета и вечный перевод
между ними.

Поэтому тип цвета переезжает в **этап 2** (модель), а управление цветом целиком
— перевод по профилям, экранная цветопроба, экспорт — становится отдельным
этапом 14. Разделены они потому, что тип нужен рано и всем, а движок ICC —
большая самостоятельная работа.

### 7.4. Символы — по-иллюстраторски

Определение символа хранится в документе один раз, на холсте стоят экземпляры,
правка определения меняет все экземпляры сразу.

Следствие: экземпляр — **ссылка, а не копия**. У него своя трансформация, но
геометрия общая. Нужны «разорвать связь» (экземпляр становится обычными
объектами) и «переопределить символ». Это отдельный этап 13; при желании его
можно вытащить раньше, он зависит только от модели этапа 2.

---

## 8. Что эти ответы добавляют к этапам

К **этапу 2** (модель) добавляются:

- [x] `artboards: Artboard[]` вместо флага; создание, перемещение,
      переименование — **сделано этапом 15** (`document.ts`'s
      `createVectorDocument`, `artboards: []`/`activeArtboardId: null`,
      плюс `artboard-ops.ts`'s `createArtboardAt`/`duplicateArtboard`/
      `renameArtboard`), просто не отмечено здесь тогда же — этот раздел
      честно перепроверен 6 сентября 2026, а не переписан по памяти.
- [x] Разрешение документа и единица отображения; модуль перевода единиц с
      тестами на каждую пару — **уже было к моменту этой перепроверки**:
      `document.ts`'s `resolution`/`displayUnit` поля, `units.ts`'s
      `LengthUnit`/`toPixels`/`fromPixels`/`parseLength`/`formatLength`.
- [x] `Color` как структура в ядре, миграция строковых цветов версии 2 —
      **уже было**: `packages/kernel/src/color.ts`'s `Color`/`ColorSpace`
      (`"srgb"|"cmyk"|"gray"|"lab"|"spot"`)/`colorToCss`/`cssToColor`/
      `colorToHex`, общее для растра и вектора с самого начала (см. этап
      14 выше, где этот же файл уже упоминается).
- [x] Поля ввода понимают `10мм`, `2.5cm`, `0.5in`, `12pt` — **уже было**:
      `apps/web/src/ui/atoms/number-input.ts`'s `parseNumericInput`
      понимает суффиксы `mm`/`мм`/`cm`/`in`/`pt` через `pixelsPerInch`,
      `NumberBox`/`OptionRow` уже прокидывают этот параметр по всему
      проекту, не только в векторе.

К **этапу 6** (оформление) добавляется:

- [x] **Плашечные цвета и палитры документа — сделано и проверено
      6 сентября 2026.** Новое поле `palette: PaletteColor[]` на самом
      `VectorDocumentState` (`schemaVersion` 6→7, миграция по тому же
      шаблону, что и `activeArtboardId` в этапе 15 — `палитра` по
      умолчанию пустой массив, ничего не выдумывается) — принципиально
      отдельно от `ColorPickerDialog.tsx`'s собственного списка «свотчей»
      в `localStorage` (общий на весь браузер, не сохраняется и не
      загружается вместе с документом): `palette` — часть самого
      документа. Новый файл `palette-ops.ts`
      (`addPaletteColor`/`removePaletteColor`/`renamePaletteColor`/
      `updatePaletteColor`) плюс `reseedPaletteIdCounter`, подключённая в
      `kernel.ts` тем же способом и по той же причине, что и
      `reseedShapeIdCounters` из раздела 9 этого документа (свежая
      сессия не должна намайнить id, который уже занят загруженной из
      хранилища палитрой). Панель «Палитра» (`windows/definitions/palette.ts`
      + `PalettePanel` в `DockLayout.tsx`) — добавить текущий
      foreground-цвет, кликнуть по свотчу, чтобы сделать его текущим,
      переименовать (`window.prompt`, как и переименование монтажной
      области), удалить.

      **Найден и исправлен ещё один реальный баг того же класса, что уже
      был в этапе 15** (там — с `artboards`/`activeArtboardId`, здесь — с
      новым полем `palette`): `changeVectorDocument`'s собственный
      `VectorSnapshot` и `working`-копия документа не включали `palette`
      вообще, так что кнопка «Добавить текущий цвет» в панели визуально
      ничего не делала — мутация происходила на `working`, но
      before/after-diff в `assignVectorSnapshot` полностью игнорировал
      это поле, и результат никогда не долетал до реального документа.
      Пойман не по коду, а по живой проверке в браузере: прямой клик по
      кнопке (и через `computer`, и через `element.click()` в JS,
      исключив проблему с координатами) не менял `document.state.palette`
      вообще. Исправлено так же, как и в прошлый раз — `palette` добавлен
      в `VectorSnapshot`, `snapshotVector`, `assignVectorSnapshot` и в
      обе копии (`before`/`working`) внутри `changeVectorDocument`.
      Проверено живьём после фикса: добавление и удаление цвета в панели
      теперь реально меняют документ. Тесты: `palette-ops.test.ts`
      (6 тестов), `migration.test.ts` (+1 тест на пустую палитру у
      мигрированного документа), `windows/registry.test.ts` (обновлён
      список окон вектора).

### Этап 13. Символы и экземпляры

Сделано 6 сентября 2026.

- [x] Определение символа в документе; экземпляр как ссылка с трансформацией
      — определение это обычная `group`-фигура в `state.shapes`, но с
      родителем `SYMBOLS_ROOT_ID` вместо корня документа или реальной
      группы (`types.ts`), а не второе, отдельно устроенное дерево;
      `instance`-фигура хранит только `symbolId` и свой `transform`,
      никакой копии геометрии
- [x] Правка определения меняет все экземпляры — в буквальном смысле
      автоматически: экземпляр при рендере и замере читает содержимое
      определения напрямую (`state.shapes`, отфильтрованные по
      `parentId === symbolId`), копии физически не существует, поэтому
      «синхронизировать» нечего
- [x] «Разорвать связь» (`detachInstance`) и «Переопределить символ»
      (`redefineSymbolFromShapes`) — обе в `symbol-ops.ts`, проверены живьём
      в браузере (создание символа, второй экземпляр, разрыв связи, undo
      всей цепочки — см. ниже)
- [x] Панель символов (`SymbolsPanel`, вкладка «Символы» в меню «Окно»,
      скрыта по умолчанию) — создать из выделения, разместить экземпляр,
      переопределить из выделения, разорвать связь
- [x] Тест: сто экземпляров одного символа не хранят сто копий геометрии —
      `symbol-ops.test.ts`, сравнение с «наивным» документом из 100
      независимых копий той же 200-точечной фигуры: версия с символом
      получилась примерно в 30 раз меньше сериализованного JSON

Честно о границах этой реализации:

- **Экспорт в SVG** — сделан по-настоящему, не заглушкой: символ становится
  реальным `<symbol>`, каждый экземпляр — `<use href="#…">`
  (`vector-svg-export.ts`), а не разворачивается в N копий разметки.
  Обратный путь (импорт стороннего SVG с `<symbol>`/`<use>`) через `usvg`
  честно не восстанавливает связь — usvg разворачивает `<use>` при парсинге,
  так что импортированный файл получает обычные независимые фигуры, не
  символы. Это ограничение библиотеки импорта, а не этого этапа.
- **Нет режима изоляции** («войти в символ», редактировать его содержимое
  прямо на холсте с двойным кликом, как в Illustrator/Figma). Правка
  определения сейчас — это либо «Переопределить из выделения» (нарисовать
  новое рядом и перенести в определение), либо прямая работа с деревом фигур
  программно. Обе полностью рабочие, но живого «двойной клик → редактируешь
  оригинал → выходишь» пока нет — отдельная, немаленькая UI-задача
  (полноценный canvas/artboard контекст редактирования), не начатая.
- **Вложенные символы** (символ, использующий экземпляр другого символа)
  корректно обрабатываются рендером, замером границ, хитестом и SVG-экспортом
  (последний — через цикл до стабилизации, `exportVectorDocumentToSvg`'s own
  doc comment), но отдельного теста на этот конкретный случай нет — только
  ручная проверка кода.
- Побочный эффект правильной реализации: `translateShape` теперь двигает и
  обычную верхнеуровневую `group`-фигуру (раньше для `"group"` не было ветки
  вообще — перетаскивание такой фигуры молча ничего не делало; это не
  проявлялось раньше, поскольку `shapeAt` никогда не возвращает `group` саму
  по себе).

### Этап 14. Управление цветом и CMYK

Начато 6 сентября 2026, направление sRGB→CMYK дописано в тот же день
после прямого запроса владельца («но конвертацию ргб цмик тоже нужно
сделать») — этап закрыт полностью, без открытых технических тупиков.

- [x] Порт управления цветом в ядре, общий с растром — уже был: `Color`/
      `ColorSpace`/`colorToCss` в `packages/kernel/src/color.ts` (сделано
      раньше этого этапа, тем не менее общее для обеих сред с самого начала).
      Что добавлено этим проходом — `crates/vector-color`, новый WASM-крейт
      с настоящей ICC-математикой поверх `qcms` (библиотека цветоуправления
      Mozilla, используется в Firefox; чистый Rust, собирается в
      `wasm32-unknown-unknown` без танцев с фичами — проверено напрямую) и
      `moxcms` (см. ниже)
- [x] `cmyk_to_srgb` — реальный перевод CMYK → sRGB через настоящий ICC
      CMYK-профиль (не формула `(1-c)(1-k)`, которую `colorToCss` использует
      как честно помеченную заглушку при отсутствии профиля), плюс
      `validate_icc_profile` для проверки байтов профиля перед сохранением
      его как ассета
- [x] **`srgb_to_cmyk` — обратное направление, найденное вторым проходом
      поиска доноров, а не изобретённое самостоятельно.** Первый проход
      этого этапа честно зафиксировал тупик: `qcms` строит трансформ
      только для пары `(CMYK, RGB8)`, никогда для обратной (видно прямо в
      его собственном `transform_create`), потому что решает другую
      задачу — *показ* CMYK-картинки на экране, а не превращение
      произвольного sRGB в печатопригодные проценты краски (это требует
      политики gamut mapping/генерации чёрного, то есть ровно того, ради
      чего ICC-профиль хранит отдельные таблицы A2B и B2A). Второй поиск,
      сделанный по прямому запросу владельца, нашёл `moxcms`
      (`https://github.com/awxkee/moxcms`, BSD-3-Clause OR Apache-2.0,
      чистый Rust, две зависимости — `num-traits` и `pxfm`, обе тоже
      чистый Rust, без C/FFI) — универсальный ICC-движок, который, в
      отличие от `qcms`, читает у профиля именно **B2A**-тег (PCS →
      устройство), а не только A2B. Little CMS остаётся промышленным
      эталоном и остаётся недостижим на этой платформе (C-библиотека, та
      же стена wasm32-unknown-unknown, что и Clipper2 в этапе 7) — но
      «ни одной чисто-Rust библиотеки для этого направления не существует»
      это исследование больше не подтверждает.
- [x] **Найденная и исправленная ловушка, не только новая функция.**
      `moxcms::Layout::Cmyka` — тот вариант enum, который *выглядит*
      правильным для CMYK-стороны трансформа, типизируется без единой
      ошибки — но реально даёт `InvalidLayout` в рантайме. Правильный
      вариант — `Layout::Rgba` (у `moxcms` в исходниках даже есть
      комментарий «Cmyk8 uses the same layout as Rgba8», объясняющий
      почему, — но это не то, что подсказывает само имя `Cmyka`).
      Подтверждено не чтением документации, а прямым нативным `cargo
      test`-пробником (`crates/vector-color`, временный `#[cfg(test)]`
      модуль, впоследствии удалённый) против собранного профиля —
      ровно тот урок про «код кажется верным, а реальность — нет», что
      уже записан в `CLAUDE.md`, раздел 2.
- [x] **Вторая находка при той же проверке: `qcms` и `moxcms` не
      совместимы по порядку байт в CLUT одного и того же профиля.**
      Профиль, закодированный `moxcms`'s собственным `ColorProfile::
      encode()` и затем прочитанный `qcms`, дал `cmyk_to_srgb(0,0,0,0)` и
      `cmyk_to_srgb(0,0,0,255)` **побайтово идентичными** — то есть qcms
      вообще не видел изменения по оси k. Это не баг ни в одной из
      библиотек (каждая правильно читает то, что сама же и написала бы),
      а генуинная несовместимость двух независимо написанных ICC-движков
      на уровне внутреннего порядка адресации углов LUT. Решение — не
      чинить эту несовместимость, а не полагаться на неё: `cmyk_to_srgb`
      тестируется только на профиле, собранном вручную в самом файле
      теста (исходная, проверенная фикстура этапа, не тронута),
      `srgb_to_cmyk` — только на профиле, собранном через `moxcms`'s
      `ColorProfile::encode()` (новые `build_test_cmyk_profile*` в
      `crates/vector-color`) — направления никогда не смешивают чужую
      для себя фикстуру.
- [x] Тест на реальность для `cmyk_to_srgb` (`apps/web/src/vector-color.test.ts`,
      исходная фикстура этапа, без изменений): собственноручно собранный
      минимальный валидный ICC v2 CMYK-профиль (`A2B0` тег типа `lut8Type`,
      2 точки сетки на канал). Белый (0,0,0,0) заметно светлее чёрного
      (0,0,0,1); три разных чистых краски дают три разных результата;
      значение на полпути между двумя точками сетки даёт результат
      строго между их значениями — доказательство настоящей
      многомерной интерполяции, не «16 фиксированных ответов».
- [x] Тест на реальность для `srgb_to_cmyk`, той же дисциплиной: профиль
      собран через `build_test_cmyk_profile` (обёртка над `moxcms`'s
      `encode()`, не второй самописный ICC-байт-райтер — первая попытка
      написать B2A-тег вручную в TypeScript провалилась с непрозрачной
      `InvalidProfile` от `moxcms` даже после проверки каждого поля
      заголовка, которое `moxcms` действительно валидирует; вместо
      второй попытки угадать формат — использован уже проверенный
      энкодер той же библиотеки, которая это будет читать в проде).
      Проверено: разные sRGB дают разные CMYK; значение на полпути между
      точками сетки — строго между их значениями, не совпадает ни с
      одной; профиль без B2A-тега честно даёт `None`, не подменяется
      напрасно.
- [x] ICC-профили: импорт + экранная цветопроба — сделано и проверено
      6 сентября 2026. Панель Палитра получила раздел «Цветопроба»:
      выбор из уже импортированных ICC-профилей (список берётся из
      `kernel.assets.list()`, отфильтрованный по `meta.icc === true`),
      кнопка импорта нового файла (`validateIccProfile` отклоняет файл
      раньше, чем он попадёт в хранилище, `kernel.assets.importAsset` с
      `kind: "binary"` — та самая уже существовавшая инфраструктура,
      которой не хватало только UI-пути), и чекбокс «Цветопроба на
      экране».

      `VectorDocumentState.cmykProfileAssetId`/`softproof`
      (schemaVersion 9→10, честная миграция: `null`/`false` — у v9
      документа то же самое поведение и так уже было, экрана без
      цветопробы). Добавлены сразу в `VectorSnapshot` и все три места,
      которые уже дважды забывали новое поле в этой сессии (artboards,
      palette) — на этот раз предотвращено заранее, а не найдено заново.

      Архитектура: `packages/env-vector/src/softproof.ts`'s
      `collectSolidPaintColors` — чистая, синхронная, тестируемая функция
      (собирает уникальные sRGB-цвета сплошных заливок/обводок по всему
      документу; градиенты и не-sRGB пространства сознательно не
      охвачены — честная узость масштаба, той же формы, что уже есть у
      `colorToCss`'s собственных lab/spot). Асинхронная половина —
      `apps/web/src/vector-softproof.ts`'s `useCmykSoftproof` — не может
      жить в `env-vector` (нужен WASM `srgbToCmyk`/`cmykToSrgb`, который
      сломал бы границу слоёв раздела 5 CLAUDE.md), зеркалит уже
      существующий `useModifierResults` (`vector-modifiers.ts`) один в
      один: тот же cache-by-revision контракт (пересчёт раз на правку, не
      раз на кадр), тот же откат к настоящему значению, пока асинхронный
      результат не готов, вместо пустого/чёрного экрана. `VectorWorkspace
      .tsx`'s `renderShape`/`renderShapeTree` получили третий параметр
      (`proofColors`), подставляющий проверенный цвет по точному
      css-ключу, который уже посчитал `resolveAppearance` — никакого
      второго кодирования цвета, которое могло бы разойтись с первым.

      Проверено живьём (реальный поток, не прямая мутация модели):
      настоящий валидный ICC-профиль собран через `buildTestCmykIccProfile`
      (тот же тестовый билдер, что использует `vector-color.test.ts`),
      загружен через настоящий `<input type="file">` (синтетический
      `File`+`DataTransfer`, реальное событие `change`) — профиль появился
      в выпадающем списке, `cmykProfileAssetId` документа обновился.
      Добавлена тестовая фигура с заливкой `rgba(200, 50, 50, 1)`; чекбокс
      «Цветопроба» нажат по-настоящему — фактическая заливка в живом SVG
      сменилась на `rgba(254, 255, 255, 1)` (результат прогона через
      тестовый профиль); повторный клик по чекбоксу вернул исходный цвет.

      Тесты: 4 новых в `softproof.test.ts` (сбор цветов, дедупликация,
      пропуск градиентов/не-sRGB, пустой документ), 1 в `migration.test.ts`.
      1112 тестов зелёные, `tsc --noEmit` чисто на всех 4 пакетах.

      Честно не сделано в этом проходе: цветопроба градиентов (каждый
      стоп градиента можно было бы прогонять так же, но это отдельная
      работа не только над цветом, но и над `resolveAppearance`'s
      обработкой `<linearGradient>`/`<radialGradient>`), CMYK-экспорт
      (PDF/SVG с внедрённым профилем) — цветопроба показывает превью
      на экране, но не меняет, что уходит в файл при экспорте; это
      осталось честной дырой ровно там, где план её и оставлял, просто
      теперь рядом с ней есть работающая цветопроба, а не только
      математика без UI.
- [ ] Сверка с эталонными плашками, а не «на глаз» — сама строка была
      частью этого этапа с самого начала (`7dfd2e0`), но при более поздней
      правке этого же файла оказалась оторвана от секции и провисела в
      самом конце документа, никак не связанная с текстом вокруг неё, до
      6 сентября 2026, когда её нашли и вернули на место. **Не сделано, и
      не техническая задача**: сверка с эталонными плашками (например,
      физической книжкой образцов Pantone) требует физического объекта
      или заведомо доверенного цифрового эталона рядом с монитором — то,
      что не существует внутри этого репозитория и не может быть
      автоматизировано агентом, работающим только с кодом и браузером.
      `srgbToCmyk`/`cmykToSrgb` (`crates/vector-color`) уже проверены
      измерением — разные sRGB дают разные CMYK, значение на полпути между
      точками сетки строго между их значениями (см. этот же этап выше) —
      но это проверка внутренней согласованности математики, не проверка
      «совпадает ли то, что видно на экране, с реальной краской». Решение
      этого пункта — задача владельца с реальным принтером и книжкой
      образцов, не то, что можно закрыть из этой сессии.

### Этап 15. Настоящий Canvas: артборды как у Illustrator, а не как «единственная страница»

Добавлено 6 сентября 2026 по прямому разбору владельца, сравнивавшего
устройство рабочей области в Illustrator с тем, что уже в VRAVIO. Найден
реальный архитектурный пробел, не просто недоделанная функция.

**Модель данных уже правильная.** `Artboard { id, name, x, y, width,
height }`, отдельный список `VectorDocumentState.artboards: Artboard[]`,
фигуры не вложены в артборд — ровно принцип Illustrator «монтажная
область размечает холст, но не владеет объектами», зафиксированный ещё на
этапе 2 (раздел 7.1 этого документа). Один объект имеет полное право
лежать наполовину на артборде A, наполовину на B, или вообще между ними —
модель это уже допускает.

**Настоящий косяк — отсутствие самого Canvas.** У Illustrator это
отдельная, гораздо большая сущность («paperboard»/«infinite canvas» —
у Adobe ограничено физически, но на порядки больше типичного документа, с
режимом Large Canvas до ~2270×2270 дюймов), на которой артборды — просто
прямоугольники где угодно. У VRAVIO такой сущности сейчас нет вовсе:
`state.width`/`state.height` — это одновременно и «размер документа», и
жёсткая граница, за которую физически ничего не может выйти:

```
apps/web/src/VectorWorkspace.tsx:421
<svg width={state.width} height={state.height} viewBox="0 0 {state.width} {state.height}">
```

Второй артборд с `x: 2000` в `artboards[]` окажется вне `viewBox` —
невидим и недостижим указателем, а не «объект вне активного артборда, но
на canvas», как должно быть. Сейчас документ VRAVIO ведёт себя как один
артборд Illustrator, а не как его canvas. Из этого пробела прямо
вытекают все недостающие куски ниже — они не сделаны не потому что
забыты, а потому что для них физически нет фундамента.

Сделано 6 сентября 2026.

- [x] Развязать «размер документа для экспорта по умолчанию» и «границы,
      за которые ничего не может выйти» — `computeCanvasBounds`
      (`artboard-ops.ts`): объединение дефолтной области документа, всех
      артбордов и мировых границ всех фигур верхнего уровня, с запасом.
      Не константа — растёт вместе с документом, тот же принцип, что у
      Penpot (см. ниже). `<svg viewBox>` и сама `.vector-stage` теперь
      покрывают эти границы, а не `state.width`/`state.height`
- [x] `activeArtboardId` на документе (schemaVersion 5→6, честная
      миграция: старый документ получает `null`) — активный артборд
      обозначен отдельной рамкой (`vector-artboard-outline.active`),
      «уместить в окне» ссылается на него через новую версию fit-эффекта
      в `VectorWorkspace.tsx`
- [x] Artboard Tool (`vector.artboard`, `B`): создание протяжкой,
      перемещение протяжкой по существующему прямоугольнику, клик —
      активация без перемещения (не отменяемая правка, тот же принцип,
      что у выбора фигуры). **Изменение размера через ручки, X/Y/W/H в
      панели свойств, preset, ориентация — не сделаны в этом проходе**,
      честная дыра: создание+перемещение+удаление уже дают рабочую
      систему артбордов, но не полный набор ручного редактирования
      Illustrator
- [x] Панель Artboards: список, «уместить в окне», переименование (через
      `window.prompt` — рабочее, но не встроенное текстовое поле),
      дублирование, удаление (артворк остаётся), экспорт по одному
      артборду — параллельно панели слоёв
- [x] `Move Artwork with Artboard` — чекбокс в панели параметров
      инструмента, включает поиск артворка, пространственно
      пересекающегося с прямоугольником артборда **на момент начала
      перетаскивания** (`shapesIntersectingRect`, вызванный один раз в
      `onPointerDown`, а не заново на каждый `onPointerMove`), не
      постоянное владение — `object.artboardId` не существует
- [x] Reorder отдельно от Rearrange (Grid by Row/Column) — сделано и
      проверено 6 сентября 2026. Это два разных инструмента Illustrator, и
      совмещать их было бы ошибкой: «Reorder» просто меняет порядок в
      списке артбордов (номера, порядок в экспорте) и никогда не трогает
      `x`/`y` ни одного прямоугольника, а «Rearrange» переставляет сами
      прямоугольники в сетку. `artboard-ops.ts`: `reorderArtboard(state, id,
      direction)` — своп соседних элементов в `state.artboards`;
      `rearrangeArtboardsGrid(state, count, spacing, layout, moveArtwork)` —
      раскладывает все артборды в равномерную сетку (размер ячейки — по
      самому большому артборду, чтобы никто не перекрылся, тот же
      компромисс, что и у диалога Illustrator), `layout: "row"` заполняет
      `count` штук в ряд перед переносом на следующую строку, `"column"` —
      то же по столбцам. Переиспользует `moveArtboard` (не отдельная копия
      логики переноса), так что `moveArtwork` подключается через ту же
      единственную дверь, что и обычное перетаскивание одного артборда.
      Панель Artboards: кнопка «Упорядочить все» (два `window.prompt` —
      число в ряд/столбце и отступ, затем `window.confirm` про перенос
      артворка — тот же честный, но не полированный `window.prompt`-подход,
      что уже был у переименования) и по паре кнопок ↑/↓ на строку
      артборда для Reorder, тот же визуальный паттерн, что у
      FillRow/StrokeRow в `AppearancePanel.tsx`. Тесты: 6 новых в
      `artboard-ops.test.ts` (своп с обеих сторон, no-op на границах списка,
      сетка по строкам и по столбцам с числами руками, сохранение/перенос
      артворка). Живая проверка в браузере: три артборда, разбросанные
      вручную (500,0)/(1000,0)/(0,0), Reorder (клик ↑ у второго) —
      прочитан `state.artboards` напрямую, порядок сменился, `x`/`y` не
      изменились; затем «Упорядочить все» с count=2, spacing=20 (диалоги
      отвечены программной подменой `window.prompt`/`window.confirm` —
      реальный клик по настоящей кнопке, не прямая мутация модели) —
      прочитана фактическая разметка живого SVG (`<rect
      class="vector-artboard-outline">`), три прямоугольника оказались
      ровно в (0,0), (120,0), (0,120), что совпадает с ручным расчётом
      (origin по минимуму исходных координат, ячейка 100+20=120). Полный
      прогон — 1094 теста, все зелёные; `tsc --noEmit` чисто на всех
      4 пакетах.
- [x] Линейки, направляющие, `rulerOrigin`, режим линеек Global/Artboard и
      `Guide { scope }` — сделано и проверено 6 сентября 2026, закрывая заодно
      и честную дыру этапа 5 (у вектора вообще не было линеек/направляющих).

      Взято не с нуля — `raster-ruler-guides.tsx` уже решал ровно эту задачу
      для растра (раздел 1 CLAUDE.md: сначала искать в своём же репозитории),
      и его же собственный комментарий уже объясняет, почему этот хром обязан
      жить вне масштабируемого слоя (`non-scaling-stroke` не отменяет
      CSS-трансформацию зума на родителе). Не копия: координатная математика
      другая (у растра документ (0,0) — это левый верхний угол канваса
      фиксированного размера; у вектора видимая область — динамический
      `computeCanvasBounds`, а `.vector-stage` центрируется вокруг **своего
      собственного** центра, не документа), и `VectorGuide` несёт то, чего
      у `RasterGuide` нет: `scope` — направляющая либо глобальная
      (`scope: null`), либо привязана к одной конкретной монтажной области.

      Ключевое архитектурное решение: **`rulerOrigin`/`rulerMode` — это
      только подпись**, не смещение. Экранная позиция направляющей и любой
      риски линейки всегда считается по чистым координатам документа, той же
      формулой, что и у растра; `rulerOrigin`/режим "artboard" влияют
      исключительно на **число, напечатанное рядом с риской** (значение
      минус эффективное начало отсчёта), а не на то, где риска физически
      стоит. Перепутать это было бы багом на следующий день: сохранённая
      позиция направляющей означала бы разное в зависимости от того, какое
      начало отсчёта было выставлено в момент чтения.

      `packages/env-vector/src/guide-ops.ts` (новый файл): `addGuide`,
      `removeGuide`, `clearGuides(scope?)` (без аргумента — все, с
      `scope` — только с этим), `visibleGuides` (глобальные плюс те, что
      привязаны к активной области), `setRulerOrigin`, `setRulerMode`.
      `apps/web/src/vector-ruler-guides.tsx` (новый файл, хук
      `useVectorRulerGuides`) — линейки и направляющие как оверлей поверх
      `.vector-stage`, той же немасштабируемой конвенции, что и курсор кисти
      в растре; жест «тащить из угла линеек» ставит `rulerOrigin`
      (только в режиме "global" — режим "artboard" всегда следует за
      активной областью и не редактируется руками), двойной клик по углу
      сбрасывает в `null`.

      `VectorDocumentState.schemaVersion` 8→9: `guides: []`, `rulerOrigin:
      null`, `rulerMode: "global"` — та же честная миграция, что и у
      предыдущих добавлений в этой сессии. `VectorSnapshot` (`vector-
      commands.ts`) сразу получил `guides`/`rulerOrigin`/`rulerMode` в этом
      же коммите, а не после третьего повторения бага «забыли поле» — тот
      же ровно баг, что уже дважды находили в этой сессии (artboards,
      palette), на этот раз предотвращённый заранее, а не найденный заново.

      Панель Artboards получила переключатель режима линеек и кнопку
      «Очистить направляющие».

      Два бага найдено и починено живой проверкой в браузере, не тестами
      (`Overlay`-подобный код этого проекта принципиально не рендерится в
      тестах — `nodes.tsx`'s собственная давняя причина): (1) двойной клик
      по направляющей для удаления не срабатывал — `.guide-overlay` несёт
      `pointer-events:none` на контейнере (общий CSS с растром, которому
      это не мешало, поскольку у его направляющих нет своего клика),
      починено точечным `pointerEvents:"stroke"` на самой линии, а не
      снятием `none` со всего оверлея; (2) перетаскивание из угла линеек
      не ставило `rulerOrigin` вовсе — `cornerPointerDown` не вызывал
      `setPointerCapture`, так что `pointermove` переставал доходить до
      обработчика, как только курсор покидал крошечный 18×18 угол —
      тот же класс бага, что CLAUDE.md §2 уже документирует для
      `setPointerCapture`, только наоборот: здесь не синтетическое
      событие путали с настоящим, а настоящий жест не получал capture,
      который ему был нужен.

      Проверено живьём: включены «Показывать линейки»/«Показывать
      направляющие», созданы линейка+направляющая реальным перетаскиванием
      (`computer`'s `left_click_drag`, не синтетические события), прочитано
      состояние документа напрямую — направляющая появилась с правильной
      позицией и `scope`, привязанным к активной области в режиме
      "artboard". Переключение Global↔Artboard подтверждено числами: одна и
      та же риска на экране (левый край `40px`) показывала "0" в режиме
      "artboard" и "300" в режиме "global" — то самое "только подпись,
      не смещение", проверенное не чтением кода, а сравнением показаний.
      Перетаскивание из угла линеек поставило `rulerOrigin`, метки на
      линейке сместились соответственно; двойной клик по углу сбросил его
      в `null`. Двойной клик по направляющей удалил её из `state.guides`.
      Кнопка «Очистить направляющие» в панели очистила обе (глобальную и
      привязанную к области) одним нажатием.

      Тесты: 8 новых в `guide-ops.test.ts`, 3 в `migration.test.ts`
      (миграция 8→9). Снапшот-цепочка (`vector-commands.ts`,
      `contract.test.ts`, `pen.test.ts`) обновлена во всех местах, где
      раньше уже дважды забывали новое поле. Полный прогон — 1107 тестов,
      все зелёные; `tsc --noEmit` чисто на всех 4 пакетах; обязательная
      проверка `grep -rn "vectorEffect=\"non-scaling-stroke\"..."` из
      раздела 1 CLAUDE.md запущена — новых совпадений нет, все линейки и
      направляющие явно нарисованы вне `.vector-stage` с буквальными
      CSS-пикселями, не полагаясь на `non-scaling-stroke`.

      Честно не сделано: привязка объектов к направляющим при перетаскивании
      (`resolveSnapForBounds` уже умеет привязку к сетке и объектам, но не
      к направляющим) — отдельная задача, не часть инфраструктуры линеек
      самой по себе; сохранение настроек показа линеек/направляющих per-tool
      не переопределяется (глобальная настройка `preferences`, общая с
      растром, как и было задумано).
- [x] Bleed — сделано и проверено 6 сентября 2026. `Artboard.bleed: number`
      (schemaVersion 7→8, честная миграция: у уже существующего артборда
      `bleed` не было — `0`, а не выдуманный отступ, восстанавливает то же
      самое поведение, что уже было). `VectorWorkspace.tsx` рисует пунктирную
      рамку вокруг артборда, отступив на `bleed` во все стороны, только
      когда `bleed > 0` (не пунктир поверх уже нарисованной сплошной линии
      у артборда без вылета) — экранная толщина линии `1/zoom`, тот же
      немасштабируемый интерфейс, что и у остальной обводки артборда
      (раздел 1 CLAUDE.md). Панель Artboards: кнопка с полем вылета
      (`window.prompt`, тот же честный, но не полированный подход, что и у
      переименования), подсвечивается, когда вылет не нулевой. «Экспорт
      этой области» включает вылет в область экспорта по умолчанию — так
      же, как реальная настройка вылета Illustrator влияет на то, что
      реально попадает на страницу при печати; не отдельная галочка,
      которая могла бы ничего не делать (раздел 3 CLAUDE.md), раз у
      артборда без вылета `crop` совпадает с его собственным прямоугольником
      один в один.

      Заодно найден и починен настоящий, независимый от bleed баг в этой же
      области: `document.ts`'s `reseedShapeIdCounters` — уже существующая
      починка коллизии id при восстановлении документа (see раздел 9) —
      поднимала счётчик артбордов не в том модуле. У `document.ts` есть
      собственный, фактически неиспользуемый в приложении `createArtboard`
      со своим счётчиком (используется только при создании нового
      документа с дефолтным артбордом, `store.ts`), а реальный путь
      создания артборда — Artboard Tool и панель Artboards — минтит id
      через `artboard-ops.ts`'s `createArtboardAt`, у которого счётчик
      **свой, отдельный**, и `reseedShapeIdCounters` его не трогал вовсе.
      То есть сессия, восстановившая документ с `artboard-3`, могла
      создать новый артборд Artboard-инструментом и получить коллизию с
      `artboard-1`/`artboard-2` — тот же класс бага, что уже дважды находили
      и чинили в этой сессии для фигур (`nextId`) и палитры, просто не
      закрытый для артбордов, хотя комментарий у `reseedShapeIdCounters`
      утверждал обратное. Починено новой `reseedArtboardIdCounter`
      (`artboard-ops.ts`), подключённой в `kernel.ts` рядом с двумя уже
      существующими вызовами реseed-функций на том же единственном
      «документ входит в сессию» стыке.

      Тесты: 3 новых в `migration.test.ts`/`document.test.ts`-стиле для
      миграции, 2 в `artboard-ops.test.ts` для `setArtboardBleed`
      (отрицательное значение зажимается в 0, копия наследует вылет
      оригинала), 2 для `reseedArtboardIdCounter` (поднимает счётчик,
      никогда не понижает). Живая проверка в браузере: артборд с
      `bleed: 10`, прочитана фактическая разметка живого SVG
      (`<rect class="vector-artboard-bleed" x="-10" y="-10" width="120"
      height="120">`, координаты сошлись с ручным расчётом), кнопка вылета
      в панели нажата по-настоящему (`window.prompt` подменён программно,
      реальный клик по кнопке) — значение в модели сменилось на 25,
      разметка живого SVG обновилась вместе с ним. Полный прогон — 1099
      тестов, все зелёные; `tsc --noEmit` чисто на всех 4 пакетах.
- [x] Экспорт по конкретному артборду — `exportVectorDocumentToSvg`
      получил необязательный параметр `crop`: `viewBox` смещается на
      прямоугольник артборда, фигуры пишутся в реальных координатах
      документа (SVG сам обрезает лишнее по `viewBox`, функции не нужно
      фильтровать фигуры). **Только SVG** — PNG/JPEG/PDF по артборду не
      сделаны
- [x] Проверка, ради которой всё затевалось — проверено живьём в
      браузере, не только тестами: два артборда в разных концах canvas,
      третий объект физически между ними — все остаются полноценными
      редактируемыми фигурами одного дерева (клик по фигуре вне всех
      артбордов по-прежнему работает, подтверждено через `activeShapeId`
      после клика); `Move Artwork` выключен — двигается пустой
      прямоугольник; включён — фигуры внутри двигаются на ту же дельту,
      что и артборд (подтверждено сравнением координат до/после)

**Реальный баг, найденный этой же живой проверкой, не тестами.** Кнопка
«Удалить» в панели Artboards визуально ничего не делала. Причина —
`changeVectorDocument`/`VectorSnapshot` (`vector-commands.ts`) появились
до артбордов и знали только про `shapes`/`activeShapeId`/`selection`;
`before`/`after`-снимок для отмены строился именно из этих трёх полей,
поэтому мутация, трогающая только `working.artboards` (создание,
перемещение, дублирование, удаление, переименование артборда — вообще
любая функция `artboard-ops.ts`), полностью терялась в момент записи
`after` обратно в документ. Создание и перемещение артборда через
инструмент при этом выглядели рабочими, потому что `context.mutate`
пишет в живой документ напрямую, минуя этот снимок — ломался только сам
факт истории (undo) и любой путь, который ЦЕЛИКОМ полагается на
`changeVectorDocument` для применения эффекта, как кнопки панели.
Починено: `VectorSnapshot` теперь несёт `artboards`/`activeArtboardId`,
и `snapshotVector`/`changeVectorDocument`'s собственный `before`/`after`
тоже. Проверено: undo восстанавливает удалённый артборд обратно.

**Исследование перед реализацией** (по прямому указанию — «изучи
опенсорс сначала»): Penpot (открытый исходный код, тот же SVG-в-браузере
стек, что и у VRAVIO) держит canvas «практически бесконечным» — тайловый
кэш рендера, который у них есть, это оптимизация отрисовки больших
документов, а не часть модели координат. Это подтвердило правильное
направление: `computeCanvasBounds` растёт от содержимого, а не является
жёсткой константой — тот же принцип в дешёвой для текущего масштаба
VRAVIO форме, без собственного тайлового рендера (он не нужен при тех
размерах документов, что у VRAVIO сейчас, и остаётся честной дырой на
случай, если понадобится).

---

## 9. Долг: инструмент «Перо» — обзор возможностей и жестов

Добавлено 6 сентября 2026 года по внешнему обзору (владелец переслал разбор
от стороннего ассистента). Математическая основа пера уже в порядке —
настоящий кубический Безье, `handleIn`/`handleOut` с зеркалированием, весь
нарисованный контур попадает в историю одной операцией. Не хватает жестов и
интеграции с уже существующей инфраструктурой (снаппинг, Node Tool), а не
модели данных. Донор жестов — **Krita** (Bezier Curve Tool) и **Inkscape**
(pen-tool.cpp/node-tool.cpp), оба под GPL: см. раздел 4 — перечень
возможностей, не код.

Первый приоритет (без этого перо не ощущается профессиональным инструментом)
— **сделано и проверено 6 сентября 2026** (донор поведения — Krita Bézier
Curve Tool и Inkscape `pen-tool.cpp`/`node-tool.cpp`, оба названы в этом
разделе выше; Inkscape закрывает контур хит-тестом по `SPDrawAnchor` у
стартовой точки, а не угадыванием пиксельного радиуса — тот же принцип
воспроизведён здесь через уже существующую константу допуска `nodes.tsx`'s
`tolerance = 6 / zoom`, не вторую независимо придуманную цифру):

- [x] Замыкание контура кликом по первой точке, а не только через ПКМ →
      Close Path — `closePath()` в `pen.tsx` уже был готов, не хватало
      жеста/hit-теста по первой точке курсором; добавлено, требует минимум
      2 точки в контуре (не даёт «замкнуть» одну точку саму на себя)
- [x] Резиновая линия (rubber-band preview) следующего сегмента между
      последней точкой и курсором в `Overlay` пера — прямая линия, если у
      последней точки ещё нет `handleOut`, настоящая кубическая кривая,
      если есть (совпадает с тем, что реально нарисует коммит сегмента)
- [x] `Shift` во время клика/протяжки ручки — фиксация угла сегмента и ручки
      кратно 45° (`ToolPointer` уже нёс модификатор, `pen.tsx` его не
      читал) — 45°, а не 15°, как в некоторых других редакторах: значение
      взято из самой этой спецификации, не переоткрыто заново
- [x] `Alt` во время протяжки новой точки — сразу отрывает `handleOut` от
      `handleIn`, независимый corner point (та же конвенция `mirror =
      !pointer.altKey`, что уже была в `nodes.tsx` — переиспользована, не
      изобретена заново под другим именем)
- [x] Снаппинг — `context.snapping` уже передавался каждому инструменту
      (этап 5), но `pen.tsx` его не использовал вообще; теперь новая точка
      (не при зажатом Shift) снаппится через `resolveSnapForBounds` с
      нулевым размером в точке клика — тот же вызов, что уже делает
      `vector.select`'s драг, не вторая реализация снаппинга для одной точки

**Найдено при живой проверке в браузере, не входит в исходный список выше —
реальный баг коллизии id, не проблема жеста.** Свежая перезагрузка страницы
сбрасывает модульный счётчик `nextId` (`packages/env-vector/src/document.ts`)
в 0, а уже загруженный из хранилища документ несёт фигуры с id, выданными
счётчиком *предыдущей* сессии — первый же новый клик пера в свежей сессии
может намайнить id, который уже занят загруженной фигурой; `addShape` не
проверяет коллизии (то же допущение «документы маленькие, коллизий не
бывает», которое годами было верным просто потому, что раньше ничего не
могло дать коллизию). Результат — новая точка тихо дописывается в массив
точек **чужой, уже существующей** фигуры вместо создания новой. Почищено
явным доводом: `reseedShapeIdCounters(state)` (новая функция, `document.ts`)
поднимает оба счётчика (фигур и артбордов) выше максимума, уже присутствующего
в документе, вызывается один раз при восстановлении сессии
(`apps/web/src/kernel.ts`, сразу после `autosave.restore()`) — единственная
дверь, через которую персистентный документ вообще возвращается в живую
сессию, вне зависимости от среды. Тесты: `packages/env-vector/src/document.test.ts`
(3 теста, включая «счётчик не понижается более плотным документом позже»).

**Второй найденный баг того же захода — ограничение самого инструмента
проверки, не кода.** Живой драг через `computer`'s `left_click_drag` с
`modifiers: "alt"` не удержал Alt через все синтетические `pointermove`
внутри одного жеста — итоговая ручка оказалась зеркальной, как без Alt
вообще. Headless-тест на реальных вызовах `onPointerDown`/`onPointerMove`
(`apps/web/src/environments/vector/tools/pen.test.ts`) подтвердил, что код
работает верно (`handleIn` остаётся `undefined`, не зеркалится) — тот же
урок, что уже записан в этом файле и в `CLAUDE.md` несколько раз: синтетический
ввод в браузерном тесте может маскироваться под настоящий ровно до первого
места, где это важно, здесь — до продолжительности удержания модификатора
через несколько кадров жеста.

Второй приоритет:

- [x] **Временный Node Tool через `Ctrl`/`Cmd` — сделано и проверено
      6 сентября 2026.** `hitTestNode`/`nodePosition` и сама мутация
      (`applyNodeMove`) вынесены из `nodes.tsx` в экспортируемые функции —
      `pen.tsx` зовёт их напрямую, а не переизобретает hit-test или логику
      переноса ручки/якоря второй раз (заодно и сам `nodes.tsx` переведён
      на `applyNodeMove`, чтобы не было двух копий одной мутации). Работает
      и когда путь ещё рисуется (правит точки/ручки именно черновика, не
      трогая `draft`/`handle` — рисование продолжается как ни в чём не
      бывало после отпускания Ctrl), и когда перо простаивает (правит
      активную фигуру документа, как обычный Node Tool). Удержание Ctrl
      над пустым холстом честно проваливается в обычное поведение пера
      (ставит точку), а не тихо проглатывает клик. Тесты — 3 новых в
      `pen.test.ts`: правка узла активной фигуры, правка ручки
      рисуемого черновика с последующим продолжением рисования,
      Ctrl-клик по пустому месту. **Живьём в браузере не подтверждено с
      той же точностью, что тесты** — тот же класс ограничения точности
      клика по конкретному узлу/ручке в 6-пиксельном допуске при
      автоматизации через `computer`, что уже задокументирован в этом
      разделе выше (см. заметку про `Alt`); headless-покрытие точное
      и достаточное, честно записано как единственный вид проверки для
      этого конкретного пункта.
- [x] **Добавление узла кликом по существующему сегменту, удаление — кликом
      по существующему узлу, без выхода из инструмента «Перо» — сделано и
      проверено 6 сентября 2026.** Геометрия — новый общий модуль
      `packages/env-vector/src/path-segment-ops.ts` (`closestPointOnPath`/
      `insertPointOnPathSegment`), не завязан на `pen.tsx` специально:
      настоящее разбиение кубической кривой по Де Кастельжо, а не прямая
      линия к середине хорды — искривлённый сегмент после добавления узла
      сохраняет ровно ту же форму по обе стороны от новой точки, только
      разбитую на два куска (доказано тестом: сэмпл исходной кривой в
      четверти параметра совпадает с серединой первой половины после
      разбиения). У ближайшей точки на кривой нет замкнутой формулы (это
      многочлен 5-й степени) — используется сэмплирование (32 точки на
      сегмент), тот же практичный компромисс, которым пользуется
      «добавить узел» в любом другом редакторе. Удаление узла кликом
      обрабатывается раньше продолжения контура (клик по любой точке
      уже существующего пути, кроме продолжаемого конца открытого пути);
      удаление до 2 точек убирает путь целиком, а не оставляет вырожденную
      1-точечную «фигуру». Тесты: `packages/env-vector/src/path-segment-ops.test.ts`
      (9 тестов на саму геометрию) + 4 новых в `pen.test.ts` (удаление
      внутренней точки, удаление до полного исчезновения фигуры, добавление
      узла на прямом сегменте, пересчёт тестов на закрытый/однoточечный
      путь — обе ситуации теперь корректно ловятся удалением узла раньше,
      чем «начать новый путь», что и обнаружило устаревшее ожидание в двух
      старых тестах).
- [x] **Состояния курсора — сделано и проверено 6 сентября 2026, второй
      приоритет полностью закрыт этим пунктом.** Новый опциональный метод
      `cursorFor?(context, pointer): string | undefined` на самом контракте
      `VectorToolDefinition` (`types.ts`), не хук специально под перо — любой
      будущий инструмент с несколькими разными исходами одного клика может
      использовать тот же механизм. `VectorWorkspace.tsx` зовёт его на
      каждый `onPointerMove` и применяет как инлайн-стиль `cursor` на сам
      `<svg>`, сбрасывая при смене инструмента и уходе курсора с холста.
      Точных нативных CSS-курсоров под «закрыть путь» или «удалить точку»
      не существует — выбраны разумные приближения (`grab` для
      Ctrl-редактирования узла и продолжения контура, `alias` для
      замыкания, `not-allowed` для удаления, `copy` для добавления узла,
      `undefined`/обычный `crosshair` для обычной новой точки), это лучше,
      чем полное отсутствие подсказки, но не универсальный стандарт — так
      и записано в комментарии кода. `cursorFor` **не** переиспользует код
      `onPointerDown` буквально (это было бы более честным «единственной
      дверью», но потребовало бы более крупного рефакторинга уже
      протестированной логики клика) — он зеркалит тот же порядок проверок
      вручную, что и явно помечено в комментарии как принятый компромисс:
      функция чтения курсора может ошибиться дёшево (один кадр неверной
      подсказки), а не дорого, как ошибка в самой мутации. Проверено живьём
      в браузере на всех четырёх нетривиальных состояниях (`not-allowed`
      над обычной точкой, `grab` над продолжаемым концом открытого пути,
      `copy` над серединой сегмента, обычный `crosshair` вне фигуры) через
      `getComputedStyle`, не только на глаз — плюс 5 headless-тестов в
      `pen.test.ts`.
- [x] **Продолжение существующего открытого контура кликом по его концу —
      сделано и проверено 6 сентября 2026.** Клик рядом с последней точкой
      незамкнутого пути (тот же допуск в 6 экранных пикселей, что и у
      замыкания) подхватывает этот путь как черновик вместо создания нового
      через `createShape()` — сам клик-подхват не добавляет точку (иначе
      рядом с существующим концом появлялась бы вырожденная точка почти
      там же), только следующий клик реально продлевает контур; ручка
      сразу заякорена на существующей последней точке, так что клик-драг
      на подхвате может вытянуть из неё новую ручку, как у любой другой
      уже поставленной точки. **Соединение двух открытых контуров в один —
      всё ещё не сделано**, честная недоделка: подхват сейчас работает
      только с тем контуром, что рисуется в данный момент (`draft`), а не
      с двумя независимо завершёнными путями. Прикреплять новую точку
      только к *концу* контура (последняя точка), не к началу — тоже
      честная дыра: продолжение с начала потребовало бы разворота порядка
      точек и обмена `handleIn`/`handleOut` местами, не сделано в этом
      проходе. Тесты — `apps/web/src/environments/vector/tools/pen.test.ts`
      (2 новых: подхват без дублирования точки, отказ подхвата у
      закрытого/однoточечного пути); живая проверка в браузере отдельно
      подтвердила, что резиновая линия корректно стартует от подхваченной
      точки, а Escape во время подхваченного жеста откатывает контур ровно
      к состоянию до подхвата (не теряет исходные точки).

Третий приоритет, после первых двух:

- [x] **Умное преобразование угловой/сглаженной точки, автосглаживание —
      сделано и проверено 6 сентября 2026.** `toggleCornerSmooth` (новая
      функция в `path-segment-ops.ts`, общая с `vector.nodes`, не только
      пером) переключает угловую точку (без ручек) в сглаженную и обратно.
      Автосглаживание — не выдумка с нуля, а стандартная эвристика,
      которую в том или ином виде используют и Illustrator (Convert Anchor
      Point), и Inkscape («сделать узел гладким»): касательная — направление
      от предыдущего соседа к следующему, длина каждой ручки — треть
      расстояния до соответствующего соседа. У конечной точки открытого
      контура (сосед только с одной стороны) ручка появляется только с
      этой одной стороны — выдумывать вторую значило бы увести контур
      наружу за его собственный конец, чего не делает ни один редактор.
      Жест — `Ctrl`+двойной клик по самой точке (не по ручке) во временном
      Node Tool из этой же сессии; проверить живьём в браузере с той же
      точностью, что тестами, не удалось — тот же класс ограничения
      автоматизации (Ctrl, удерживаемый через синтетический двойной клик),
      что уже задокументирован выше в этом разделе для `Alt`; headless
      покрытие (`path-segment-ops.test.ts`, 5 тестов на саму функцию,
      `pen.test.ts`, 1 тест на жест) точное и принято как единственная
      проверка для этого конкретного пункта.
- [x] **Точный числовой ввод координаты при размещении точки — сделано и
      проверено 6 сентября 2026, третий приоритет и весь раздел 9
      полностью закрыты этим пунктом.** Новая для проекта UI-конструкция:
      `<foreignObject>` внутри того же `<svg>`, что и остальной оверлей
      пера (ни у одного инструмента раньше не было текстового поля прямо
      на холсте — раздел 2.2 тз.md просит аналогичное окошко для
      растровых фигурных инструментов, но там оно ещё не сделано ни разу,
      этот пример можно будет взять оттуда, когда дойдёт очередь). Ввод
      открывается тем же приёмом, что и во многих редакторах — начал
      печатать цифру, `-` или `.`, пока путь рисуется — а не спрятан в
      меню; собирает сырой текст `"x, y"` и разбирает его только по
      Enter, так что недопечатанное значение никогда не двигает точку
      молча. Escape, пока поле открыто, закрывает только его (без потери
      набранного текста для остального пути) — отдельная проверка внутри
      того же глобального слушателя, который иначе понимает Escape как
      «удалить весь черновик», поймала бы это неправильно, если бы не
      стояла раньше него. Размер поля — `140/zoom × 28/zoom` в
      координатах документа, то есть ровно 140×28 экранных пикселей на
      любом зуме (тот же приём, что и `strokeWidth={1/zoom}` у остальных
      элементов оверлея, — интерфейс не масштабируется зумом, см.
      `CLAUDE.md` раздел 1). Проверено живьём в браузере по-настоящему
      сквозным сценарием, не только тем, что что-то отрендерилось:
      напечатана цифра (клавиша `key`, не `type` — `type` этого
      инструмента посылает текст не через отдельные `keydown`, которые
      слушает этот код, что и обнаружилось при первой попытке), поле
      появилось; значение `"300, 250"` дошло до `changeDocument`, и точка
      документа реально переместилась в `(300, 250)`; Escape отдельно
      проверен — закрывает поле, оставляя точку и весь путь как есть, не
      стирая черновик целиком. **Не покрыто headless-тестами** — логика
      живёт внутри JSX `Overlay`, а не в экспортируемой функции, и этот
      проект принципиально не рендерит `Overlay` в тестах вообще
      (`nodes.tsx`'s собственный комментарий об этом же), так что живая
      проверка в браузере — единственная и достаточная для этого пункта.
- [x] **Удаление узла с сохранением кривизны контура (не просто прямая
      между соседями) — сделано и проверено 6 сентября 2026.**
      `deletePointPreservingCurve` (тот же общий модуль) — тоже честно
      эвристика, не точная переподгонка кривой (для двух произвольных
      кубических Безье через удаляемую точку в общем случае не существует
      одной кубической, которая бы в точности повторила обе): у каждого
      выжившего соседа направление касательной остаётся тем же, что было
      (собственная ручка со стороны, обращённой к удаляемой точке, никуда
      не делась), только длина ручки пересчитывается под новую (обычно
      бо́льшую) хорду до другого выжившего соседа — та же пропорция «треть
      расстояния», что и в автосглаживании выше. Сосед без ручки с нужной
      стороны (угловая точка) так и остаётся угловым — соединение прямой
      линией, без придуманной кривизны, которой не было. Подключено сразу
      в двух местах, а не только в `pen.tsx`: `vector.nodes`'s собственный
      Delete/Backspace тоже переведён на эту функцию вместо прежнего
      простого `.filter()` — правило CLAUDE.md §4 про единственную дверь,
      раз оба места делают семантически одно и то же удаление узла.
      Тесты: 3 новых в `path-segment-ops.test.ts` (пересчёт длины ручки под
      новую хорду, сохранение угловой точки угловой, отсутствие мутации
      входного массива).

**Проверено 6 сентября 2026 (эта сессия) — не баг.** `pen.tsx`
действительно берёт цвет из `context.foregroundColor`, а не
`context.options.color`, как и `shape-drag.ts` (rectangle/ellipse) и
`text.ts` — но это не мёртвый чекбокс: `App.tsx:504` принудительно
подставляет `effectiveForegroundColor` в `values.color`, который уходит в
панель, а `onChange` для `id === "color"` вызывает
`store.setForegroundColor(...)`, а не `store.setToolOption`. То есть
свотч «Цвет» у любого инструмента с такой опцией — это живое зеркало
глобального foreground-цвета, а не независимая настройка конкретного
инструмента; читать `context.foregroundColor` напрямую в этих трёх файлах
— правильно и соответствует контрактному тесту
(`contract.test.ts`'s собственный комментарий это уже подтверждал для
растровых инструментов, просто не был перепроверен для вектора). Была
предпринята попытка «исправить» чтением `context.options.color` — сломала
контрактный тест `has no option that leaves the result unchanged`,
что и вскрыло настоящую картину; откачено обратно.

Отдельно стоит перепроверить обработку двойного клика: сейчас
`pointerDown` сначала всегда добавляет новую точку и только потом проверяет
`detail >= 2` для завершения контура — есть риск, что второй клик двойного
клика успевает добавить точку почти в той же позиции перед завершением,
оставляя вырожденный нулевой последний сегмент.
