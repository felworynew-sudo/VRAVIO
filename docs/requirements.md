# VRAVIO requirements ledger

This ledger is the traceability bridge between the source documents in `D:\Данные\Редактор` and implementation. Status values: `planned`, `in-progress`, `verified`, `blocked`.

| ID | Requirement | Source | Status | Implementation | Verification |
|---|---|---|---|---|---|
| KERNEL-DOC-001 | Documents of all environments live outside React state and expose revisions | ТЗ 1.2.1; GPT 1, 4 | verified | `packages/kernel/src/document-store.ts`, `apps/web/src/useDocuments.ts`; Zustand stores IDs only | `kernel.test.ts`, `store.test.ts`, typecheck |
| KERNEL-HIST-001 | Undo/redo uses reversible commands and transactions | ТЗ 1.2.3; GPT 4 | in-progress | `packages/kernel/src/history-manager.ts` | `history-manager.test.ts` |
| KERNEL-CMD-001 | One command registry drives menus, shortcuts and command palette | ТЗ 1.2.5; GPT 4 | in-progress | `packages/kernel/src/command-registry.ts`, `apps/web/src/commands.ts` | registry tests; browser keyboard test pending |
| KERNEL-ASSET-001 | Binary assets are content-addressed and deduplicated | ТЗ 1.2.2; GPT 1 | in-progress | `packages/kernel/src/asset-store.ts` | `asset-store.test.ts` |
| SHELL-001 | Open and switch Raster, Vector, Audio and Video documents | ТЗ 2.3, Stage 0; GPT 11 | in-progress | `apps/web/src/App.tsx` | store tests; browser test pending |
| SHELL-002 | Environment accent is a 2 px tab stripe, not a colored fill | ТЗ 2.3.2; GPT 11 | in-progress | `apps/web/src/styles.css` | visual test pending |
| SHELL-003 | Options bar is generated from active tool option schema | ТЗ Stage 0; GPT 4 | in-progress | `apps/web/src/tools.ts`, `apps/web/src/App.tsx` | typecheck; interaction test pending |
| SHELL-004 | Command palette opens with Mod+K | ТЗ D.3; GPT 11 | in-progress | `apps/web/src/App.tsx` | UI test pending |
| SHELL-005 | Dark/light/high-contrast themes use semantic tokens; focus, canvas surround and all environment accents are customizable and persisted | ТЗ 1.4, D.4; Интерфейс редактора: Тема | in-progress | `styles.css`, `store.ts`, `App.tsx`, `SettingsDialog.tsx` | typecheck; clean dev-server boot; visual test pending |
| SHELL-007 | One searchable Settings window covers interface, performance, convenience, guides and shortcuts; language selector reserves RU/EN/UK/ES/DE/JA/ZH | Интерфейс редактора: Настройки; user clarification 2026-09-01 | in-progress | persisted schema in `store.ts`; categorized UI in `SettingsDialog.tsx`; untranslated strings use explicit EN fallback | typecheck; browser interaction pending |
| SHELL-NEWDOC-001 | Raster/Vector creation opens a compact preset dialog with editable dimensions, px/mm/cm/in conversion, orientation, resolution, background and pixel aspect | Инструменты Растр РС: О создании документа; GPT 5.11 | in-progress | `NewDocumentDialog.tsx`, `store.ts`; initial preset catalog covers photo/print/art/web/mobile/video | metadata/unit tests; visual interaction pending |
| SHELL-NEWDOC-002 | Unsupported CMYK and 16/32-bit modes are not falsely advertised as working | GPT Appendix B; ТЗ color pipeline | verified | options remain visibly disabled until corresponding storage/render pipeline exists | typecheck |
| RASTER-COLOR-001 | 16-bit integer and 32-bit float documents are mandatory future pipelines, with precision-preserving storage, filters, compositing and export | Инструменты Растр РС: О создании документа; user clarification 2026-09-01 | planned | requires typed tile formats beyond current `Uint8ClampedArray` and golden fixtures for every operation | pending |
| RASTER-COLOR-002 | CMYK is mandatory future color mode with ICC conversion/soft proof/export, not an sRGB label alias | Инструменты Растр РС; GPT Appendix B; user clarification 2026-09-01 | planned | platform color-management port and ICC engine required | pending |
| SHELL-006 | Panels are dockable and the workspace layout survives reload | ТЗ 2.4, Stage 0; GPT 11 | in-progress | `apps/web/src/DockLayout.tsx` using Dockview core/react | browser reload test pending |
| SHELL-BRAND-001 | Environment accents and icons use customizable Raster #a100ff, Vector #0068ff, Audio #ffb600, Video #ff0000 tokens | Интерфейс редактора: цветовое кодирование | in-progress | `styles.css`, `EnvironmentIcon.tsx`; SVG masks from `D:\SVG` | visual test pending |
| SHELL-BRAND-002 | Full logo is used where unobtrusive; workspace shell uses the compact color-plaque logo | User clarification 2026-09-01 | in-progress | `App.tsx`; `логотип белый.svg`, `логотип цветная плашка.svg` | asset HTTP check; visual test pending |
| RASTER-MODEL-001 | Raster document owns ordered layers and RGBA buffers outside React | ТЗ 4.1; GPT 5.1, 5.10 | in-progress | `packages/env-raster/src/document.ts` | `raster.test.ts`; browser integration pending |
| RASTER-PAINT-001 | Brush and eraser share a pressure-aware mask pipeline and commit one history action per gesture | ТЗ 4.3; GPT 5.2 | in-progress | `packages/env-raster/src/paint.ts`, `apps/web/src/RasterWorkspace.tsx` | pixel tests; pointer test pending |
| RASTER-PAINT-002 | Fast strokes preserve coalesced pointer trajectory, use smooth curve resampling and do not synchronously repaint for every input event | ТЗ 2.6, 4.3; user bug report 2026-09-01 | in-progress | coalesced events + quadratic resampling + requestAnimationFrame preview; single-layer fast path in `RasterWorkspace.tsx` | curve unit test; latency browser benchmark pending |
| RASTER-VIEW-001 | Navigation transforms the rendered canvas and includes Hand Tool (Рука), Zoom Tool (Масштаб), Rotate View Tool (Вращение вида), temporary Space-hand, Fit on Screen and Actual Size 100% | Интерфейс редактора: Инструменты навигации; ТЗ 2.6, Stage 1, D.3 | in-progress | per-document viewport state and CSS transform path in `store.ts`, `RasterWorkspace.tsx`; commands and shortcuts in `commands.ts`, `App.tsx` | store tests; browser interaction pending |
| RASTER-VIEW-002 | Every raster tool inverts pan, zoom and rotation before consuming pointer coordinates | ТЗ 2.6, 4.3 | in-progress | shared inverse view transform in `RasterWorkspace.tsx` | typecheck; rotated-canvas pointer test pending |
| RASTER-FILL-001 | Contiguous paint bucket uses iterative flood fill with tolerance | GPT 5.3, 5.6 | in-progress | `packages/env-raster/src/fill.ts` | `raster.test.ts` |
| RASTER-TOOLS-001 | The Stage 1 raster baseline includes working Move, rectangular/elliptical/lasso/color selections, Brush, Pencil, Highlighter, Eraser, Fill, Eyedropper, Crop and Type tools | ТЗ Stage 1; Инструменты Растр РС 1–41 | in-progress | `tools.ts`, `RasterWorkspace.tsx`, `selection.ts`, `transform.ts`, `paint.ts`; text currently commits raster pixels and will become a non-destructive text layer with the layer-type milestone | 14 raster unit tests; browser interaction pending |
| RASTER-ICON-001 | User-authored SVGs are used directly when present, without guessed substitutes | User asset update 2026-09-01 | in-progress | `Заливка.svg`, `Пипетка.svg`, `КАРАНДАШ.svg`, `ЛАССО.svg`, `ВОЛШЕБНАЯ ПАЛОЧКА.svg`, navigation SVGs connected in `tools.ts`; eraser SVG filename not present in `D:\SVG` at scan time | asset HTTP checks; visual verification pending |
| RASTER-SAMPLE-001 | Eyedropper samples point or averaged merged composition | GPT 5.4 | in-progress | `packages/env-raster/src/render.ts`, `RasterWorkspace.tsx` | average test; interaction pending |
| RASTER-SELECT-001 | Pixel selection is an alpha mask with cached bounds and replace/add/subtract/intersect arithmetic | ТЗ 4.4; GPT 5.3 | in-progress | `packages/env-raster/src/selection.ts`, `RasterWorkspace.tsx` | 3 selection unit tests; pointer interaction pending |
| RASTER-SELECT-002 | Rectangular marquee supports feathering, marching overlay, undo/redo, select all, deselect and invert | Инструменты Растр РС; ТЗ 4.4 | in-progress | `selection.ts`, `RasterWorkspace.tsx`, `commands.ts` | typecheck + unit tests; browser keyboard test pending |
| RASTER-SELECT-003 | Active alpha selection constrains brush, eraser and paint bucket; Shift/Alt modify marquee combination | Инструменты Растр РС: selection behavior | in-progress | selection mask passed into shared paint/fill pipeline; pointer modifiers in `RasterWorkspace.tsx` | 2 clipping unit tests; browser pointer test pending |
| RASTER-TRANSFORM-001 | Free Transform offers Skew/Distort/Perspective/Warp, not just scale/rotate | ТЗ; GPT 5.11 | verified | `quadLayerPixels`/`quadSelection` homography engine in `env-raster/src/transform.ts`; quad and mesh-warp gesture handling, right-click mode menu in `RasterWorkspace.tsx` | typecheck; unit tests; browser: all four modes dragged, committed and undone with byte-identical restore |
| ROUNDTRIP-VECTOR-001 | A raster image placed in a vector document can be opened, edited and saved back in the raster environment, with the vector document picking up the change | ТЗ 1.2.1 (round-trip); Вектор РС | verified | `VectorShape` `image` kind (`pixelAssetId` reference) in `env-vector/src/types.ts`; `VectorEnvironment` in `apps/web/src/vector-environment.ts`; drag-and-drop import and right-click "Edit in Raster Environment" in `VectorWorkspace.tsx` | typecheck; unit tests; browser: dropped image → edited in linked raster tab → applied via Mod+Shift+Enter (fixed a real Return/Enter shortcut bug found in testing) → vector image updated, verified pixel-exact |
| RASTER-CAMERARAW-001 | RAW camera files (CR2/NEF/ARW/DNG/…) open through a real demosaic decoder, with a Camera Raw adjustment filter | ТЗ 5.11; Растр РС | verified | `libraw-wasm` (already vendored via npm) in `rawDecode.ts`, `CameraRawDialog.tsx`, `App.tsx` File > Open and Filter > Camera Raw Filter | browser: genuine 3474×2314 DNG sample decoded via LibRaw-Wasm with real demosaiced pixel data (not the embedded-JPEG fallback) |
| RASTER-3D-001 | A persistent 3D layer type: text extrusion, model import (OBJ/GLB), extrude-another-layer, live rotation and lighting editable afterward in Properties | ТЗ; Слои растр (Camera Raw / 3D layer icon) | verified | `Scene3DLayerData` on `RasterLayer` in `env-raster/src/types.ts`; `three.js` (already vendored via npm) + OBJLoader/GLTFLoader/TextGeometry in `scene3d-render.ts`; commands and Properties-panel controls in `scene3d-commands.ts`, `DockLayout.tsx`; drag-and-drop `.obj/.glb/.gltf` import in `RasterWorkspace.tsx` | typecheck; unit tests; browser: text/model/extrude sources all rendered and visually confirmed; rotation edit + undo verified byte-identical after fixing a trimmed-buffer bug in the undo snapshot |
| RASTER-PSD-001 | Reading .psd/.psb files: layers, bounds, opacity, visibility, blend mode and (Unicode) name, converted to VRAVIO raster layers | ТЗ; user request 2026-09-03 | verified | `decodePsd` (header, PackBits/RAW channel decode, layer records with `luni` Unicode name block, merged-image fallback) in `env-raster/src/psd-import.ts`; wired into File > Open in `App.tsx` | typecheck; unit tests; browser: a real 17 MB, 11-layer PSD (text, photo and shape layers) decoded and composited correctly — found and fixed a zero-size-layer-record cursor desync bug via this test. Scoped to RGB/Grayscale, 8/16/32-bit-downsampled, RAW or RLE compression; CMYK, indexed color, ZIP-compressed channels, adjustment/text/vector layer data and PSD writing are explicitly out of scope |

The full tool/filter/format/icon matrix will be expanded from the documents before each environment implementation. No feature is marked `verified` from UI presence alone.
